# Исправление ошибки при выполнении скрипта

## ❌ Неправильно:

```python
exec(open(books/create_test_data.py).read())
```

**Ошибка:** `NameError: name 'books' is not defined`

## ✅ Правильно:

```python
exec(open('books/create_test_data.py').read())
```

**Важно:** Путь к файлу должен быть **строкой в кавычках**!

## 📝 Полная последовательность команд:

1. Запустите Django Shell:
   ```bash
   python manage.py shell
   ```

2. Выполните команду (обратите внимание на кавычки):
   ```python
   exec(open('books/create_test_data.py').read())
   ```

3. Дождитесь завершения. Вы увидите сообщение:
   ```
   ТЕСТОВЫЕ ДАННЫЕ УСПЕШНО СОЗДАНЫ!
   ```

## 🔄 Альтернативный способ (без кавычек в команде):

Если вы хотите избежать проблем с кавычками, используйте этот способ:

```bash
python manage.py shell < books/create_test_data.py
```

Эта команда выполняется **вне** Django Shell, прямо в терминале.

