# 📘 Подробная инструкция по тестированию OAuth 2.0

**Для начинающих разработчиков**

**⚠️ ВАЖНО:** 
- ✅ Все команды выполняются через **терминал Cursor** (встроенный терминал редактора)
- ✅ Откройте терминал: `` Ctrl + ` `` или через меню **View → Terminal**
- ✅ Можно открыть несколько вкладок терминала (для сервера и команд)
- ✅ **Тип терминала:** Если вы видите `MINGW64` или `bash` в промпте - вы используете Git Bash
- ✅ **Для Git Bash:** используйте обратный слэш `\` для продолжения строки в многострочных командах
- ✅ **Для Windows CMD:** используйте символ `^` для продолжения строки
- ✅ **Рекомендуется:** используйте однострочные команды - они работают везде

**📝 Стандартные данные для тестирования:**
- **Суперпользователь:** Email: `admin@example.com`, Password: `1234`
- Эти данные используются во всех примерах команд

---

## 📋 Содержание

1. [Подготовка к тестированию](#подготовка-к-тестированию)
2. [Шаг 1: Запуск сервера разработки](#шаг-1-запуск-сервера-разработки)
3. [Шаг 2: Создание OAuth2 приложения в админке](#шаг-2-создание-oauth2-приложения-в-админке)
4. [Шаг 3: Получение Access Token](#шаг-3-получение-access-token)
5. [Шаг 4: Тестирование защищенного ресурса](#шаг-4-тестирование-защищенного-ресурса)
6. [Шаг 5: Обновление токена через Refresh Token](#шаг-5-обновление-токена-через-refresh-token)
7. [Шаг 6: Отзыв токена](#шаг-6-отзыв-токена)
8. [Часто задаваемые вопросы](#часто-задаваемые-вопросы)
9. [Альтернативные способы тестирования](#альтернативные-способы-тестирования)

---

## 🚀 Подготовка к тестированию

### Что вам понадобится:

1. ✅ Установленный Django проект с настроенным OAuth 2.0
2. ✅ Суперпользователь (admin) в Django
   - Email: `admin@example.com`
   - Password: `1234`
3. ✅ **Терминал Cursor** (встроенный терминал редактора) - все команды выполняются здесь
4. ✅ Браузер (Chrome, Edge, Firefox и т.д.)
5. ✅ curl (встроен в Windows 10/11) или Postman для альтернативного тестирования

**Примечание:** 
- Инструкция предназначена **только для Windows**
- **Все команды выполняются через терминал Cursor** (`` Ctrl + ` `` для открытия терминала)
- Примеры команд адаптированы для работы в терминале Cursor
- curl встроен в Windows 10/11, дополнительная установка не требуется
- Можно открыть несколько вкладок терминала для параллельной работы

### Стандартные данные суперпользователя:

Для удобства тестирования используется стандартный суперпользователь:
- **Email:** `admin@example.com`
- **Password:** `1234`

Эти данные используются во всех примерах команд в инструкции.

### Проверка установки:

**⚠️ ВАЖНО:** Перед выполнением команд убедитесь, что виртуальное окружение активировано!

#### 1. Активация виртуального окружения

**В терминале Cursor:**

```bash
# Активация виртуального окружения
source .venv/Scripts/activate
```

После активации в начале строки терминала должно появиться `(.venv)`.

**Если виртуального окружения нет, создайте его:**

```bash
# Создание виртуального окружения
python -m venv .venv

# Активация
source .venv/Scripts/activate
```

#### 2. Установка зависимостей

**Убедитесь, что виртуальное окружение активировано**, затем выполните:

```bash
# Установка всех зависимостей из requirements.txt
pip install -r requirements.txt
```

**Или используйте Python из venv напрямую (если pip не работает):**

```bash
.venv/Scripts/python.exe -m pip install -r requirements.txt
```

#### 3. Проверка миграций

```bash
# Убедитесь, что виртуальное окружение активировано
python manage.py migrate
```

**Если видите ошибку `ModuleNotFoundError: No module named 'django'`:**
- ✅ Убедитесь, что виртуальное окружение активировано (`(.venv)` в начале строки)
- ✅ Если не активировано, выполните: `source .venv/Scripts/activate`
- ✅ Установите зависимости: `.venv/Scripts/python.exe -m pip install -r requirements.txt`

---

## 📝 Шаг 1: Запуск сервера разработки

### 1.1. Откройте терминал Cursor

**В Cursor:**
- Нажмите `` Ctrl + ` `` (обратная кавычка) для открытия терминала
- Или через меню: **View → Terminal**
- Или через меню: **Terminal → New Terminal**

**Важно:** 
- ✅ Все команды выполняются в терминале Cursor
- ✅ Не нужно открывать отдельные окна PowerShell или CMD
- ✅ Можно открыть несколько вкладок терминала (для сервера и команд)

### 1.2. Перейдите в директорию проекта

**В терминале Cursor:**

```bash
cd "C:\Users\sened\Desktop\Обучение_GPT_PD\lesson_17_Templates. Работа с шаблонами"
```

**Или если терминал уже открыт в директории проекта:**
- Проверьте текущую директорию: `pwd` (или `cd` без параметров)
- Если вы уже в правильной директории, можно пропустить этот шаг

**Примечание:** 
- В терминале Cursor обычно автоматически открывается директория проекта
- Если путь содержит кириллицу, используйте кавычки

### 1.3. Активируйте виртуальное окружение (если еще не активировано)

**Перед запуском сервера убедитесь, что виртуальное окружение активировано:**

```bash
source .venv/Scripts/activate
```

Должно появиться `(.venv)` в начале строки терминала.

### 1.4. Запустите сервер разработки

**В терминале Cursor выполните:**

```bash
python manage.py runserver
```

**Ожидаемый результат:**
```
Starting development server at http://127.0.0.1:8000/
Quit the server with CTRL-BREAK.
```

✅ **Сервер запущен!** 

**Важно для терминала Cursor:**
- Сервер будет работать в текущей вкладке терминала
- Для выполнения других команд откройте **новую вкладку терминала** (кнопка "+" в терминале или `Ctrl+Shift+` `)
- Не закрывайте вкладку с сервером - он должен продолжать работать
- Для остановки сервера нажмите `Ctrl+C` в терминале с сервером

---

## 🔐 Шаг 2: Создание OAuth2 приложения в админке

### 2.1. Откройте админку Django

1. Откройте браузер
2. Перейдите по адресу: `http://127.0.0.1:8000/admin/`
3. Введите данные суперпользователя:
   - **Email:** `admin@example.com`
   - **Password:** `1234`
4. Нажмите "Войти"

### 2.2. Найдите раздел OAuth2 PROVIDER

После входа в админку вы увидите список разделов. Найдите раздел **"OAuth2 PROVIDER"**.

**Что вы увидите:**
- Applications (Приложения)
- Access tokens (Токены доступа)
- Refresh tokens (Токены обновления)
- Grants (Коды авторизации)

### 2.3. Создайте новое приложение

1. **Нажмите на "Applications"** (или "Приложения")
2. **Нажмите кнопку "Add Application"** (или "Добавить Application") в правом верхнем углу

### 2.4. Заполните форму создания приложения

**Обязательные поля:**

1. **Name** (Название):
   ```
   Test OAuth2 Client
   ```
   *Любое понятное название для вашего тестового приложения*

2. **User** (Пользователь):
   - Выберите из выпадающего списка вашего суперпользователя
   - В проекте используется кастомная модель пользователя с email вместо username
   - **ВАЖНО:** В выпадающем списке должно отображаться `admin@example.com` (email пользователя)
   - Выберите пользователя `admin@example.com` из списка
   - **Если видите ошибку "Выберите корректный вариант" после сохранения:**
     - ✅ **РЕШЕНО:** Проблема исправлена через кастомный admin для OAuth2 Application
     - Убедитесь, что сервер Django перезапущен после изменений в `shop/admin.py`
     - Обновите страницу админки (F5) или очистите кеш браузера (Ctrl+Shift+Delete)
     - Попробуйте выбрать пользователя снова и сохранить
     - Если проблема все еще сохраняется, проверьте логи сервера Django для дополнительной информации

3. **Client type** (Тип клиента):
   - Выберите: **`Confidential`**
   - *Это означает, что клиент может безопасно хранить секретный ключ*

4. **Authorization grant type** (Тип авторизации):
   - Выберите: **`Resource owner password-based`**
   - *Этот тип позволяет получать токены используя логин и пароль пользователя*

5. **Redirect URIs** (URI перенаправления):
   - Оставьте **пустым**
   - *Для данного типа авторизации не требуется*

6. **Client id** и **Client secret**:
   - Оставьте **пустыми**
   - *Они будут автоматически сгенерированы после сохранения*

### 2.5. Сохраните приложение

1. Нажмите кнопку **"Save"** (или "Сохранить") внизу формы
2. После сохранения вы будете перенаправлены на страницу списка приложений

### 2.6. Скопируйте Client ID и Client Secret

**⚠️ ВАЖНО: Как редактировать приложение в списке**

В Django админке:
- **Галочка (чекбокс)** используется для **массовых действий** (actions), а не для редактирования
- **Для редактирования** нужно **кликнуть на название приложения** (колонка "Name") или на **PK** (номер в первой колонке)
- После клика вы перейдете на страницу редактирования приложения

**Шаги:**

1. **Найдите ваше созданное приложение** в списке Applications
2. **Кликните на название приложения** (колонка "Name") или на **PK** (номер в первой колонке)
   - ⚠️ **НЕ используйте галочку** - она только для массовых действий
   - ✅ **Кликните прямо на название** или номер - это откроет страницу редактирования
3. **На странице редактирования найдите поля:**
   - **Client id** (например: `abc123def456ghi789`)
   - **Client secret** (например: `xyz789uvw012rst345`)

4. **Скопируйте оба значения** и сохраните их в безопасном месте

   ⚠️ **ВАЖНО:** 
   - Client Secret показывается только один раз при создании
   - Если вы его потеряли, создайте новое приложение
   - Не публикуйте эти данные в открытом доступе

**Если не можете кликнуть на название:**
- Убедитесь, что вы кликаете именно на текст названия, а не на галочку
- Попробуйте кликнуть на PK (номер в первой колонке)
- Обновите страницу (F5) и попробуйте снова
- Перезапустите сервер Django, если проблема сохраняется

**Пример того, что вы увидите:**
```
Client id: abc123def456ghi789jkl012mno345pqr678stu901vwx234yz
Client secret: xyz789uvw012rst345abc678def901ghi234jkl567mno890pqr
```

✅ **Приложение создано!** Теперь у вас есть Client ID и Client Secret.

---

## 🎫 Шаг 3: Получение Access Token

### 3.1. Подготовка данных

Вам понадобятся следующие данные:
- **Client ID** (скопирован из админки)
- **Client Secret** (скопирован из админки)
- **Username** (email вашего суперпользователя: `admin@example.com`)
- **Password** (пароль вашего суперпользователя: `1234`)

### 3.2. Использование curl в терминале Cursor

**Важно:** 
- Откройте **новую вкладку терминала** в Cursor (кнопка "+" в терминале)
- Сервер Django должен продолжать работать в другой вкладке
- Используйте `curl.exe` для надежности

#### Команда для терминала Cursor

**Способ 1: Однострочная команда (рекомендуется, работает везде)**

```bash
curl.exe -X POST http://127.0.0.1:8000/o/token/ -d "grant_type=password" -d "username=admin@example.com" -d "password=1234" -d "client_id=ВАШ_CLIENT_ID" -d "client_secret=ВАШ_CLIENT_SECRET"
```

**Способ 2: Многострочная команда**

**Для Git Bash (MINGW64) - используйте обратный слэш `\`:**
```bash
curl.exe -X POST http://127.0.0.1:8000/o/token/ \
  -d "grant_type=password" \
  -d "username=admin@example.com" \
  -d "password=1234" \
  -d "client_id=ВАШ_CLIENT_ID" \
  -d "client_secret=ВАШ_CLIENT_SECRET"
```

**Для Windows CMD - используйте символ `^`:**
```cmd
curl.exe -X POST http://127.0.0.1:8000/o/token/ ^
  -d "grant_type=password" ^
  -d "username=admin@example.com" ^
  -d "password=1234" ^
  -d "client_id=ВАШ_CLIENT_ID" ^
  -d "client_secret=ВАШ_CLIENT_SECRET"
```

**Пример с реальными данными (стандартный суперпользователь):**
```bash
curl.exe -X POST http://127.0.0.1:8000/o/token/ -d "grant_type=password" -d "username=admin@example.com" -d "password=1234" -d "client_id=ВАШ_CLIENT_ID" -d "client_secret=ВАШ_CLIENT_SECRET"
```

**Где взять Client ID и Client Secret:**
- См. Шаг 2: Создание OAuth2 приложения в админке
- После создания приложения скопируйте эти значения из админки
- Замените `ВАШ_CLIENT_ID` и `ВАШ_CLIENT_SECRET` на реальные значения

**Важно:** 
- Email и пароль уже указаны в примере (`admin@example.com` / `1234`)
- Вам нужно только заменить `ВАШ_CLIENT_ID` и `ВАШ_CLIENT_SECRET` на значения из админки

**Важные моменты для терминала Cursor:**
- ✅ Используйте `curl.exe` для надежности
- ✅ **Для Git Bash (MINGW64):** используйте обратный слэш `\` для продолжения строки
- ✅ **Для Windows CMD:** используйте символ `^` для продолжения строки
- ✅ **Рекомендуется:** используйте однострочную команду (Способ 1) - она работает везде
- ✅ Замените `ВАШ_CLIENT_ID` и `ВАШ_CLIENT_SECRET` на реальные значения из админки
- ✅ Email: `admin@example.com`, Password: `1234` (стандартные данные суперпользователя)
- ✅ Можно копировать команды прямо из инструкции и вставлять в терминал (Ctrl+V)

**⚠️ Если видите ошибки `bash: -d: command not found`:**
- Это означает, что вы используете Git Bash, но команда написана для Windows CMD
- Используйте однострочную команду (Способ 1) или многострочную с обратным слэшем `\` (Способ 2 для Git Bash)

### 3.3. Ожидаемый результат

**Успешный ответ (200 OK):**
```json
{
  "access_token": "abc123def456ghi789jkl012mno345pqr678stu901vwx234yz",
  "expires_in": 3600,
  "token_type": "Bearer",
  "scope": "read write",
  "refresh_token": "xyz789uvw012rst345abc678def901ghi234jkl567mno890pqr"
}
```

**Что означают эти поля:**
- `access_token` - токен для доступа к защищенным ресурсам (действителен 1 час)
- `expires_in` - время жизни токена в секундах (3600 = 1 час)
- `token_type` - тип токена (всегда "Bearer")
- `scope` - права доступа токена
- `refresh_token` - токен для обновления access_token (действителен 1 день)

### 3.4. Ошибки и их решения

#### Ошибка 1: "invalid_client"

**Ответ:**
```json
{
  "error": "invalid_client",
  "error_description": "Invalid client_id or client_secret."
}
```

**Решение:**
- Проверьте правильность Client ID и Client Secret
- Убедитесь, что скопировали их полностью (без пробелов)
- Попробуйте создать новое приложение

#### Ошибка 2: "invalid_grant"

**Ответ:**
```json
{
  "error": "invalid_grant",
  "error_description": "Invalid credentials given."
}
```

**Решение:**
- Проверьте правильность username и password
- Убедитесь, что пользователь существует и активен
- Проверьте, что в username нет лишних пробелов

#### Ошибка 3: "unsupported_grant_type"

**Ответ:**
```json
{
  "error": "unsupported_grant_type"
}
```

**Решение:**
- Убедитесь, что используете `grant_type=password`
- Проверьте, что приложение создано с типом "Resource owner password-based"

### 3.5. Сохранение токена

**Скопируйте значение `access_token`** из ответа и сохраните его. Он понадобится для следующего шага.

**Пример:**
```
access_token: abc123def456ghi789jkl012mno345pqr678stu901vwx234yz
```

✅ **Токен получен!** Теперь вы можете использовать его для доступа к защищенным ресурсам.

---

## 🔒 Шаг 4: Тестирование защищенного ресурса

### 4.1. Подготовка

Вам понадобится:
- **Access Token** (полученный на предыдущем шаге)
- Терминал с curl

### 4.2. Запрос к защищенному ресурсу

#### Вариант A: Токен в заголовке Authorization (рекомендуется)

**В терминале Cursor:**

```bash
curl.exe -H "Authorization: Bearer ВАШ_ACCESS_TOKEN" http://127.0.0.1:8000/api/oauth2/protected/
```

**Пример с реальным токеном:**
```bash
curl.exe -H "Authorization: Bearer abc123def456ghi789jkl012mno345pqr678stu901vwx234yz" http://127.0.0.1:8000/api/oauth2/protected/
```

#### Вариант B: Токен в query параметре

**В терминале Cursor:**

```bash
curl.exe "http://127.0.0.1:8000/api/oauth2/protected/?access_token=ВАШ_ACCESS_TOKEN"
```

**Пример:**
```bash
curl.exe "http://127.0.0.1:8000/api/oauth2/protected/?access_token=abc123def456ghi789jkl012mno345pqr678stu901vwx234yz"
```

**Важно:** Используйте двойные кавычки для URL с параметрами.

### 4.3. Ожидаемый результат

**Успешный ответ (200 OK):**
```json
{
  "message": "Доступ разрешен через OAuth 2.0",
  "user": "admin@example.com",
  "user_id": 1,
  "scopes": ["read", "write"],
  "token_info": {
    "token": "abc123def456ghi789...",
    "expires": "2025-01-27T15:30:00+00:00",
    "scope": "read write"
  },
  "timestamp": "2025-01-27T14:30:00+00:00"
}
```

**Что означают эти поля:**
- `message` - сообщение об успешной авторизации
- `user` - email пользователя, от имени которого выдан токен
- `user_id` - ID пользователя
- `scopes` - список прав доступа токена
- `token_info` - информация о токене (сокращенный токен, срок действия, scope)
- `timestamp` - время выполнения запроса

### 4.4. Ошибки и их решения

#### Ошибка 1: "Authentication credentials were not provided"

**Ответ:**
```json
{
  "detail": "Authentication credentials were not provided."
}
```

**Решение:**
- Убедитесь, что передали токен в заголовке или query параметре
- Проверьте правильность написания заголовка: `Authorization: Bearer TOKEN`
- Убедитесь, что между `Bearer` и токеном есть пробел

#### Ошибка 2: "Invalid token"

**Ответ:**
```json
{
  "error": "invalid_token"
}
```

**Решение:**
- Проверьте, что токен скопирован полностью (без пробелов в начале/конце)
- Убедитесь, что токен не истек (срок действия 1 час)
- Попробуйте получить новый токен

#### Ошибка 3: "Token has expired"

**Ответ:**
```json
{
  "error": "Token has expired"
}
```

**Решение:**
- Токен истек (прошло более 1 часа)
- Получите новый токен (см. Шаг 5)

### 4.5. Тестирование без токена

Попробуйте выполнить запрос **без токена**:

**В терминале Cursor:**

```bash
curl.exe http://127.0.0.1:8000/api/oauth2/protected/
```

**Ожидаемый результат:**
```json
{
  "detail": "Authentication credentials were not provided."
}
```

Это подтверждает, что ресурс действительно защищен!

✅ **Защищенный ресурс работает!** Вы успешно получили доступ к API используя OAuth 2.0 токен.

---

## 🔄 Шаг 5: Обновление токена через Refresh Token

### 5.1. Зачем это нужно?

Access Token действителен только **1 час**. После истечения срока действия вам нужно получить новый токен. Вместо повторного ввода логина и пароля можно использовать Refresh Token.

### 5.2. Подготовка данных

Вам понадобятся:
- **Refresh Token** (полученный при создании Access Token)
- **Client ID** (из админки)
- **Client Secret** (из админки)

### 5.3. Запрос на обновление токена

**В терминале Cursor:**

**Однострочная команда (рекомендуется):**
```bash
curl.exe -X POST http://127.0.0.1:8000/o/token/ -d "grant_type=refresh_token" -d "refresh_token=ВАШ_REFRESH_TOKEN" -d "client_id=ВАШ_CLIENT_ID" -d "client_secret=ВАШ_CLIENT_SECRET"
```

**Многострочная команда:**

**Для Git Bash (MINGW64) - используйте обратный слэш `\`:**
```bash
curl.exe -X POST http://127.0.0.1:8000/o/token/ \
  -d "grant_type=refresh_token" \
  -d "refresh_token=ВАШ_REFRESH_TOKEN" \
  -d "client_id=ВАШ_CLIENT_ID" \
  -d "client_secret=ВАШ_CLIENT_SECRET"
```

**Для Windows CMD - используйте символ `^`:**
```cmd
curl.exe -X POST http://127.0.0.1:8000/o/token/ ^
  -d "grant_type=refresh_token" ^
  -d "refresh_token=ВАШ_REFRESH_TOKEN" ^
  -d "client_id=ВАШ_CLIENT_ID" ^
  -d "client_secret=ВАШ_CLIENT_SECRET"
```

**Пример:**
```bash
curl.exe -X POST http://127.0.0.1:8000/o/token/ -d "grant_type=refresh_token" -d "refresh_token=xyz789uvw012rst345abc678def901ghi234jkl567mno890pqr" -d "client_id=abc123def456ghi789" -d "client_secret=xyz789uvw012rst345"
```

### 5.4. Ожидаемый результат

**Успешный ответ (200 OK):**
```json
{
  "access_token": "новый_токен_abc123...",
  "expires_in": 3600,
  "token_type": "Bearer",
  "scope": "read write",
  "refresh_token": "новый_refresh_token_xyz789..."
}
```

**Важно:**
- Вы получите **новый** Access Token и Refresh Token
- Старый Access Token будет автоматически отозван
- Новый токен действителен еще 1 час

✅ **Токен обновлен!** Теперь вы можете использовать новый Access Token.

---

## 🚫 Шаг 6: Отзыв токена

### 6.1. Зачем отзывать токен?

Отзыв токена полезен когда:
- Пользователь вышел из системы
- Токен был скомпрометирован
- Нужно принудительно завершить сессию

### 6.2. Отзыв Access Token

**В терминале Cursor:**

**Однострочная команда (рекомендуется):**
```bash
curl.exe -X POST http://127.0.0.1:8000/o/revoke_token/ -d "token=ВАШ_ACCESS_TOKEN" -d "client_id=ВАШ_CLIENT_ID" -d "client_secret=ВАШ_CLIENT_SECRET"
```

**Многострочная команда:**

**Для Git Bash (MINGW64) - используйте обратный слэш `\`:**
```bash
curl.exe -X POST http://127.0.0.1:8000/o/revoke_token/ \
  -d "token=ВАШ_ACCESS_TOKEN" \
  -d "client_id=ВАШ_CLIENT_ID" \
  -d "client_secret=ВАШ_CLIENT_SECRET"
```

**Для Windows CMD - используйте символ `^`:**
```cmd
curl.exe -X POST http://127.0.0.1:8000/o/revoke_token/ ^
  -d "token=ВАШ_ACCESS_TOKEN" ^
  -d "client_id=ВАШ_CLIENT_ID" ^
  -d "client_secret=ВАШ_CLIENT_SECRET"
```

**Пример:**
```bash
curl.exe -X POST http://127.0.0.1:8000/o/revoke_token/ -d "token=abc123def456ghi789jkl012mno345pqr678stu901vwx234yz" -d "client_id=abc123def456ghi789" -d "client_secret=xyz789uvw012rst345"
```

### 6.3. Ожидаемый результат

**Успешный ответ (200 OK):**
```json
{
  "revoked": true
}
```

### 6.4. Проверка отзыва

Попробуйте использовать отозванный токен:

**В терминале Cursor:**

```bash
curl.exe -H "Authorization: Bearer ОТОЗВАННЫЙ_ТОКЕН" http://127.0.0.1:8000/api/oauth2/protected/
```

**Ожидаемый результат:**
```json
{
  "error": "invalid_token"
}
```

✅ **Токен отозван!** Он больше не может быть использован для доступа к ресурсам.

---

## ❓ Часто задаваемые вопросы

### Q1: Где найти Client ID и Client Secret?

**A:** После создания приложения в админке:
1. Перейдите в раздел "OAuth2 PROVIDER" → "Applications"
2. Нажмите на название вашего приложения
3. Поля Client ID и Client Secret находятся в форме редактирования

### Q2: Что делать, если потерял Client Secret?

**A:** Client Secret показывается только один раз. Если вы его потеряли:
1. Создайте новое приложение в админке
2. Скопируйте новый Client ID и Client Secret
3. Удалите старое приложение (опционально)

### Q3: Как долго действует Access Token?

**A:** По умолчанию Access Token действителен **1 час** (3600 секунд). Это настраивается в `settings.py` в параметре `ACCESS_TOKEN_EXPIRE_SECONDS`.

### Q4: Как долго действует Refresh Token?

**A:** По умолчанию Refresh Token действителен **1 день** (86400 секунд). Это настраивается в `settings.py` в параметре `REFRESH_TOKEN_EXPIRE_SECONDS`.

### Q5: Можно ли использовать один токен для нескольких запросов?

**A:** Да! Один Access Token можно использовать для множества запросов, пока он не истек. Не нужно получать новый токен для каждого запроса.

### Q6: Что такое scope?

**A:** Scope определяет права доступа токена:
- `read` - только чтение данных
- `write` - запись данных
- `read write` - полный доступ

### Q7: Как проверить, что токен работает?

**A:** Выполните запрос к защищенному ресурсу с токеном. Если получили данные - токен работает. Если получили ошибку "invalid_token" - токен недействителен.

### Q8: Можно ли использовать токен в браузере?

**A:** Да! Вы можете открыть в браузере:
```
http://127.0.0.1:8000/api/oauth2/protected/?access_token=ВАШ_ТОКЕН
```

Но это небезопасно для продакшена, так как токен будет виден в истории браузера.

---

## 🛠️ Альтернативные способы тестирования

### Способ 1: Использование Postman

**Postman** - популярный инструмент для тестирования API.

#### Настройка запроса получения токена:

1. Откройте Postman
2. Создайте новый POST запрос
3. URL: `http://127.0.0.1:8000/o/token/`
4. Перейдите на вкладку "Body"
5. Выберите "x-www-form-urlencoded"
6. Добавьте поля:
   - `grant_type`: `password`
   - `username`: `admin`
   - `password`: `ваш_пароль`
   - `client_id`: `ваш_client_id`
   - `client_secret`: `ваш_client_secret`
7. Нажмите "Send"

#### Настройка запроса к защищенному ресурсу:

1. Создайте новый GET запрос
2. URL: `http://127.0.0.1:8000/api/oauth2/protected/`
3. Перейдите на вкладку "Headers"
4. Добавьте заголовок:
   - Key: `Authorization`
   - Value: `Bearer ваш_access_token`
5. Нажмите "Send"

### Способ 2: Использование Python скрипта

Создайте файл `test_oauth2.py`:

```python
import requests

# Данные для получения токена
TOKEN_URL = "http://127.0.0.1:8000/o/token/"
CLIENT_ID = "ваш_client_id"
CLIENT_SECRET = "ваш_client_secret"
USERNAME = "admin"
PASSWORD = "ваш_пароль"

# Получение токена
print("1. Получение Access Token...")
token_response = requests.post(TOKEN_URL, data={
    "grant_type": "password",
    "username": USERNAME,
    "password": PASSWORD,
    "client_id": CLIENT_ID,
    "client_secret": CLIENT_SECRET,
})

if token_response.status_code == 200:
    token_data = token_response.json()
    access_token = token_data["access_token"]
    refresh_token = token_data["refresh_token"]
    print(f"✅ Токен получен: {access_token[:20]}...")
    print(f"   Refresh Token: {refresh_token[:20]}...")
else:
    print(f"❌ Ошибка получения токена: {token_response.json()}")
    exit(1)

# Доступ к защищенному ресурсу
print("\n2. Доступ к защищенному ресурсу...")
protected_url = "http://127.0.0.1:8000/api/oauth2/protected/"
headers = {
    "Authorization": f"Bearer {access_token}"
}

protected_response = requests.get(protected_url, headers=headers)

if protected_response.status_code == 200:
    data = protected_response.json()
    print("✅ Доступ разрешен!")
    print(f"   Пользователь: {data['user']}")
    print(f"   Scopes: {data['scopes']}")
else:
    print(f"❌ Ошибка доступа: {protected_response.json()}")

# Обновление токена
print("\n3. Обновление токена через Refresh Token...")
refresh_response = requests.post(TOKEN_URL, data={
    "grant_type": "refresh_token",
    "refresh_token": refresh_token,
    "client_id": CLIENT_ID,
    "client_secret": CLIENT_SECRET,
})

if refresh_response.status_code == 200:
    new_token_data = refresh_response.json()
    new_access_token = new_token_data["access_token"]
    print(f"✅ Токен обновлен: {new_access_token[:20]}...")
else:
    print(f"❌ Ошибка обновления токена: {refresh_response.json()}")
```

**Запуск в терминале Cursor:**

```bash
python test_oauth2.py
```

**Или если Python не найден:**
```bash
py test_oauth2.py
```

**Примечание:** 
- Убедитесь, что Python установлен и добавлен в PATH
- Проверить можно командой: `python --version`
- Все команды выполняются в терминале Cursor (Ctrl+` для открытия)

### Способ 3: Использование браузера

Для простых GET запросов можно использовать любой браузер (Chrome, Edge, Firefox):

1. Откройте браузер
2. Введите в адресной строке:
```
http://127.0.0.1:8000/api/oauth2/protected/?access_token=ВАШ_ТОКЕН
```
3. Нажмите Enter

**Пример:**
```
http://127.0.0.1:8000/api/oauth2/protected/?access_token=abc123def456ghi789jkl012mno345pqr678stu901vwx234yz
```

**Примечание:** 
- Это работает только для GET запросов
- Небезопасно для продакшена (токен виден в истории браузера)
- Удобно для быстрого тестирования

---

## 📊 Чеклист тестирования

Используйте этот чеклист для проверки всех функций:

- [ ] Сервер Django запущен
- [ ] Создано OAuth2 приложение в админке
- [ ] Скопированы Client ID и Client Secret
- [ ] Получен Access Token через API
- [ ] Получен доступ к защищенному ресурсу с токеном
- [ ] Проверен отказ в доступе без токена
- [ ] Обновлен токен через Refresh Token
- [ ] Отозван токен
- [ ] Проверен отказ в доступе с отозванным токеном

---

## 🎓 Заключение

Поздравляем! Вы успешно:

1. ✅ Создали OAuth2 приложение
2. ✅ Получили Access Token
3. ✅ Использовали токен для доступа к защищенным ресурсам
4. ✅ Обновили токен через Refresh Token
5. ✅ Отозвали токен

Теперь вы понимаете основы работы с OAuth 2.0 в Django!

**Следующие шаги:**
- Изучите другие типы Grant (Authorization Code, Client Credentials)
- Настройте интеграцию с внешними провайдерами (Google, GitHub)
- Изучите систему Scope для более тонкого контроля доступа

---

**Удачи в разработке! 🚀**
