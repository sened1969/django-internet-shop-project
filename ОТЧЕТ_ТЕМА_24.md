# Отчет по теме #24 "Введение в веб-сервисы. Django REST Framework"

**Дата создания:** 2025-01-27  
**Вариант реализации:** Вариант 4 (Комбинированный подход)

---

## 📋 Содержание

1. [Описание выполненной работы](#описание-выполненной-работы)
2. [Настройка Django REST Framework](#настройка-django-rest-framework)
3. [Добавление связи между моделями Book и Category](#добавление-связи-между-моделями-book-и-category)
4. [Улучшение существующего API](#улучшение-существующего-api)
5. [Создание демо-приложения](#создание-демо-приложения)
6. [Сравнительный анализ подходов](#сравнительный-анализ-подходов)
7. [Документация Swagger](#документация-swagger)
8. [Примеры использования](#примеры-использования)
9. [Выводы](#выводы)

---

## 🎯 Описание выполненной работы

### Цель работы

Познакомиться с процессом создания и настройки API в Django с использованием Django REST Framework, изучить работу с сериализаторами, представлениями, вложенными сериализаторами и документацией Swagger.

### Выполненные задачи

1. ✅ Переход на библиотеку `drf-spectacular` для документации Swagger
2. ✅ Добавление модели `Category` и связи с моделью `Book`
3. ✅ Реализация вложенных сериализаторов (`CategoryDetailSerializer`)
4. ✅ Улучшение сериализаторов с добавлением `extra_kwargs` и `help_text`
5. ✅ Добавление `@extend_schema` декораторов к ViewSets для улучшения документации
6. ✅ Создание демо-приложения `api_demo` с моделью Article
7. ✅ Реализация APIView классов (`ListCreateAPIView`, `RetrieveUpdateDestroyAPIView`)
8. ✅ Настройка маршрутов и документации для демо-приложения
9. ✅ Сравнительный анализ подходов ViewSets vs APIView

---

## ⚙️ Настройка Django REST Framework

### 1. Установка зависимостей

Обновлен файл `requirements.txt`:

```txt
Django>=4.2,<5.0
Pillow>=10.0.0
djangorestframework>=3.14.0
djangorestframework-simplejwt>=5.3.0
drf-spectacular>=0.27.0
drf-spectacular-sidecar>=2024.11.0
```

### 2. Настройка в settings.py

**Добавлены приложения:**
```python
INSTALLED_APPS = [
    # ...
    'rest_framework',
    'rest_framework_simplejwt',
    'drf_spectacular',
    'drf_spectacular_sidecar',
    'api_demo',  # Новое демо-приложение
]
```

**Настройки REST_FRAMEWORK:**
```python
REST_FRAMEWORK = {
    'DEFAULT_AUTHENTICATION_CLASSES': [
        'rest_framework.authentication.SessionAuthentication',
        'rest_framework_simplejwt.authentication.JWTAuthentication',
    ],
    'DEFAULT_PERMISSION_CLASSES': [
        'rest_framework.permissions.IsAuthenticatedOrReadOnly',
    ],
    'DEFAULT_PAGINATION_CLASS': 'rest_framework.pagination.PageNumberPagination',
    'PAGE_SIZE': 20,
    'DEFAULT_RENDERER_CLASSES': [
        'rest_framework.renderers.JSONRenderer',
        'rest_framework.renderers.BrowsableAPIRenderer',
    ],
    'DEFAULT_SCHEMA_CLASS': 'drf_spectacular.openapi.AutoSchema',  # Для Swagger
}
```

**Настройки drf-spectacular:**
```python
SPECTACULAR_SETTINGS = {
    'TITLE': 'Books API',
    'DESCRIPTION': 'REST API для управления книгами, издательствами, магазинами, категориями и отзывами.',
    'VERSION': '1.0.0',
    'SWAGGER_UI_DIST': 'SIDECAR',
    'SWAGGER_UI_FAVICON_HREF': 'SIDECAR',
    'SWAGGER_UI_SETTINGS': {
        'deepLinking': True,
        'displayOperationId': False,
        'defaultModelsExpandDepth': 1,
        'defaultModelExpandDepth': 1,
        'docExpansion': 'list',
        'filter': True,
    },
    'COMPONENT_SPLIT_REQUEST': True,
    'SCHEMA_PATH_PREFIX': '/api/',
}
```

### 3. Настройка маршрутов Swagger

Обновлен файл `myshop/urls.py`:

```python
from drf_spectacular.views import (
    SpectacularAPIView,
    SpectacularSwaggerView,
    SpectacularRedocView,
)

urlpatterns = [
    # ...
    path('api/schema/', SpectacularAPIView.as_view(), name='schema'),
    path('api/docs/', SpectacularSwaggerView.as_view(url_name='schema'), name='swagger-ui'),
    path('api/redoc/', SpectacularRedocView.as_view(url_name='schema'), name='redoc'),
]
```

**Доступ к документации:**
- Swagger UI: `http://127.0.0.1:8000/api/docs/`
- ReDoc: `http://127.0.0.1:8000/api/redoc/`
- Schema JSON: `http://127.0.0.1:8000/api/schema/`

---

## 🔗 Добавление связи между моделями Book и Category

### 1. Создание модели Category

Добавлена модель `Category` в `books/models.py`:

```python
class Category(models.Model):
    """
    Модель категории книг.
    
    Представляет категорию, к которой относятся книги.
    Одна категория может содержать несколько книг (связь один ко многим).
    """
    name = models.CharField(
        max_length=100,
        unique=True,
        verbose_name='Название категории',
        help_text='Введите название категории книг'
    )
    
    description = models.TextField(
        blank=True,
        verbose_name='Описание категории',
        help_text='Описание категории (необязательно)'
    )
    
    class Meta:
        verbose_name = 'Категория'
        verbose_name_plural = 'Категории'
        ordering = ['name']
    
    def __str__(self):
        return self.name
```

### 2. Добавление связи в модель Book

Обновлена модель `Book` с добавлением поля `category`:

```python
class Book(models.Model):
    # ... существующие поля ...
    
    category = models.ForeignKey(
        Category,
        on_delete=models.CASCADE,
        related_name='books',
        null=True,
        blank=True,
        verbose_name='Категория',
        help_text='Категория книги'
    )
    
    # ... остальные поля ...
```

**Тип связи:** ForeignKey (один ко многим)
- Одна категория → много книг
- Обратная связь: `category.books.all()` - получить все книги категории

### 3. Создание миграции

Выполнена миграция базы данных:

```bash
python manage.py makemigrations books
python manage.py migrate
```

Создана миграция `0003_category_book_category.py`, которая:
- Создает модель `Category`
- Добавляет поле `category` в модель `Book`

---

## 🔧 Улучшение существующего API

### 1. Создание сериализаторов для Category

#### CategorySerializer (базовый)

```python
class CategorySerializer(serializers.ModelSerializer):
    """Сериализатор для модели Category."""
    
    books_count = serializers.IntegerField(source='books.count', read_only=True)
    
    class Meta:
        model = Category
        fields = ['id', 'name', 'description', 'books_count']
        read_only_fields = ['id', 'books_count']
        extra_kwargs = {
            'name': {
                'help_text': 'Название категории книг (максимум 100 символов, уникальное)'
            },
            'description': {
                'help_text': 'Описание категории (необязательно)'
            },
        }
```

#### CategoryDetailSerializer (вложенный)

```python
class CategoryDetailSerializer(serializers.ModelSerializer):
    """Сериализатор для детального представления категории с вложенными книгами."""
    
    books = BookSerializer(many=True, read_only=True)
    
    class Meta:
        model = Category
        fields = ['id', 'name', 'description', 'books']
        read_only_fields = ['id']
        extra_kwargs = {
            'name': {
                'help_text': 'Название категории книг (максимум 100 символов, уникальное)'
            },
            'description': {
                'help_text': 'Описание категории (необязательно)'
            },
        }
```

**Особенности вложенного сериализатора:**
- ✅ Использует `BookSerializer` для отображения книг внутри категории
- ✅ Автоматически включает все поля книг
- ✅ Позволяет получить категорию со всеми её книгами одним запросом

### 2. Обновление BookSerializer

Добавлено поле `category` в `BookSerializer`:

```python
class BookSerializer(serializers.ModelSerializer):
    # ... существующие поля ...
    
    category_name = serializers.CharField(source='category.name', read_only=True)
    
    class Meta:
        model = Book
        fields = [
            'id', 'title', 'author', 'published_date', 'description',
            'publisher', 'publisher_name', 'publisher_country',
            'category', 'category_name',  # Новые поля
            'stores', 'store_ids', 'reviews_count', 'avg_rating'
        ]
        extra_kwargs = {
            # ... существующие ...
            'category': {
                'help_text': 'ID категории книги'
            },
        }
```

### 3. Улучшение сериализаторов

Добавлены `extra_kwargs` с `help_text` для всех полей в сериализаторах приложения `books`.

**Преимущества:**
- ✅ Автоматическое отображение описаний в Swagger UI
- ✅ Улучшенная читаемость документации
- ✅ Помощь разработчикам при использовании API

### 4. Создание представлений для Category

#### CategoryViewSet (ModelViewSet)

```python
@extend_schema_view(
    list=extend_schema(
        summary="Получение списка категорий",
        description="Возвращает список всех категорий книг с количеством книг в каждой категории.",
        responses={200: CategorySerializer(many=True)}
    ),
    retrieve=extend_schema(
        summary="Получение информации о категории",
        description="Возвращает информацию о конкретной категории.",
        responses={200: CategorySerializer}
    ),
    create=extend_schema(
        summary="Создание новой категории",
        description="Создает новую категорию книг. Требуется роль администратора или менеджера.",
        responses={201: CategorySerializer}
    ),
    # ... остальные методы ...
)
class CategoryViewSet(viewsets.ModelViewSet):
    """
    ViewSet для работы с категориями книг.
    """
    queryset = Category.objects.annotate(
        books_count=Count('books')
    ).all()
    serializer_class = CategorySerializer
    permission_classes = [IsManagerOrReadOnly]
    filter_backends = [filters.SearchFilter, filters.OrderingFilter]
    search_fields = ['name', 'description']
    ordering_fields = ['name']
    ordering = ['name']
    
    @extend_schema(
        summary="Книги категории",
        description="Возвращает список всех книг в данной категории.",
        responses={200: BookSerializer(many=True)}
    )
    @action(detail=True, methods=['get'])
    def books(self, request, pk=None):
        """Возвращает список книг категории."""
        category = self.get_object()
        books = category.books.select_related('publisher', 'category').prefetch_related('stores', 'reviews').all()
        serializer = BookSerializer(books, many=True)
        return Response(serializer.data)
```

#### CategoryDetailView (RetrieveAPIView)

```python
@extend_schema(
    summary="Детальная категория с книгами",
    description="Получение категории с вложенными данными (книги в категории). "
                "Использует вложенный сериализатор для отображения всех книг категории.",
    responses={200: CategoryDetailSerializer}
)
class CategoryDetailView(RetrieveAPIView):
    """
    Представление для детального просмотра категории с вложенными книгами.
    
    Использует CategoryDetailSerializer для отображения категории со всеми книгами.
    """
    queryset = Category.objects.prefetch_related('books').all()
    serializer_class = CategoryDetailSerializer
    permission_classes = [permissions.AllowAny]
```

**Преимущества использования вложенного сериализатора:**
- ✅ Получение категории со всеми книгами одним запросом
- ✅ Оптимизация запросов через `prefetch_related`
- ✅ Удобный формат данных для фронтенда

### 5. Добавление @extend_schema декораторов

Добавлены декораторы `@extend_schema` и `@extend_schema_view` ко всем ViewSets для улучшения документации.

**Преимущества:**
- ✅ Подробные описания каждого метода API
- ✅ Указание типов ответов
- ✅ Профессиональный вид документации

### 6. Настройка маршрутов

Обновлен файл `books/api/urls.py`:

```python
router = DefaultRouter()
router.register(r'books', views.BookViewSet, basename='book')
router.register(r'publishers', views.PublisherViewSet, basename='publisher')
router.register(r'stores', views.StoreViewSet, basename='store')
router.register(r'reviews', views.ReviewViewSet, basename='review')
router.register(r'categories', views.CategoryViewSet, basename='category')  # Новый маршрут

urlpatterns = [
    path('', include(router.urls)),
    
    # Маршрут для детального просмотра категории с вложенными книгами
    path('categories/<int:pk>/detail/', views.CategoryDetailView.as_view(), name='category-detail-books'),
    
    # ... остальные маршруты ...
]
```

**URL структура для Category:**
- `GET /api/categories/` - список всех категорий
- `POST /api/categories/` - создание новой категории
- `GET /api/categories/{id}/` - получение категории
- `PUT /api/categories/{id}/` - полное обновление категории
- `PATCH /api/categories/{id}/` - частичное обновление категории
- `DELETE /api/categories/{id}/` - удаление категории
- `GET /api/categories/{id}/books/` - список книг категории (кастомное действие)
- `GET /api/categories/{id}/detail/` - категория с вложенными книгами

---

## 🎓 Создание демо-приложения

### 1. Модель Article

Создана простая модель для демонстрации:

```python
class Article(models.Model):
    title = models.CharField(max_length=200, verbose_name='Заголовок')
    content = models.TextField(verbose_name='Содержание')
    author = models.CharField(max_length=100, verbose_name='Автор')
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)
    is_published = models.BooleanField(default=False)
```

### 2. Сериализатор

```python
class ArticleSerializer(serializers.ModelSerializer):
    class Meta:
        model = Article
        fields = '__all__'
        read_only_fields = ['id', 'created_at', 'updated_at']
        extra_kwargs = {
            'title': {
                'help_text': 'Заголовок статьи (минимум 5 символов)'
            },
            # ... и т.д.
        }
```

### 3. APIView классы

**ListCreateAPIView:**
```python
@extend_schema(
    summary="Получение списка статей",
    description="Возвращает список всех статей. Можно создать новую статью через POST.",
    responses={200: ArticleSerializer(many=True)}
)
class ArticleListCreateAPIView(ListCreateAPIView):
    queryset = Article.objects.all()
    serializer_class = ArticleSerializer
```

**RetrieveUpdateDestroyAPIView:**
```python
@extend_schema(
    summary="Работа с конкретной статьей",
    description="Позволяет получить, обновить или удалить статью.",
    responses={200: ArticleSerializer, 204: None}
)
class ArticleDetailAPIView(RetrieveUpdateDestroyAPIView):
    queryset = Article.objects.all()
    serializer_class = ArticleSerializer
```

### 4. Маршруты

```python
urlpatterns = [
    path('articles/', ArticleListCreateAPIView.as_view(), name='article-list-create'),
    path('articles/<int:pk>/', ArticleDetailAPIView.as_view(), name='article-detail'),
]
```

**URL структура:**
- `GET /api/demo/articles/` - список статей
- `POST /api/demo/articles/` - создание статьи
- `GET /api/demo/articles/{id}/` - получение статьи
- `PUT /api/demo/articles/{id}/` - полное обновление
- `PATCH /api/demo/articles/{id}/` - частичное обновление
- `DELETE /api/demo/articles/{id}/` - удаление статьи

---

## 📊 Сравнительный анализ подходов

### ViewSets vs APIView классы

| Аспект | ViewSets (ModelViewSet) | APIView классы |
|--------|------------------------|----------------|
| **Простота использования** | ✅ Очень просто | ⚠️ Требует больше кода |
| **Функциональность** | ✅ Полный CRUD из коробки | ⚠️ Нужно реализовывать каждый метод |
| **Гибкость** | ⚠️ Меньше контроля | ✅ Полный контроль |
| **Код** | ✅ Меньше кода | ❌ Больше кода |
| **Router** | ✅ Использует Router | ❌ Явные маршруты |
| **Кастомные действия** | ✅ Легко через @action | ⚠️ Нужно создавать отдельные методы |
| **Фильтрация/Поиск** | ✅ Встроенная поддержка | ⚠️ Нужно настраивать вручную |
| **Когда использовать** | Для стандартного CRUD | Для нестандартной логики |

### Преимущества ViewSets

1. **Меньше кода:** Один класс заменяет несколько APIView классов
2. **Автоматический CRUD:** Все операции из коробки
3. **Router:** Автоматическая генерация URL
4. **Кастомные действия:** Легко добавлять через `@action`
5. **Встроенная фильтрация:** SearchFilter, OrderingFilter работают автоматически

### Преимущества APIView классов

1. **Полный контроль:** Можно точно настроить каждый метод
2. **Простота понимания:** Явные маршруты и методы
3. **Гибкость:** Легко добавить нестандартную логику
4. **Обучение:** Понятнее для начинающих

### Рекомендации

**Использовать ViewSets когда:**
- ✅ Нужен стандартный CRUD
- ✅ Требуется быстрая разработка
- ✅ Нужны кастомные действия (@action)
- ✅ Требуется фильтрация и поиск

**Использовать APIView классы когда:**
- ✅ Нужна нестандартная логика
- ✅ Требуется полный контроль над каждым методом
- ✅ Простота важнее функциональности
- ✅ Обучение новичков

---

## 📚 Документация Swagger

### Доступ к документации

После запуска сервера (`python manage.py runserver`):

1. **Swagger UI:** `http://127.0.0.1:8000/api/docs/`
   - Интерактивный интерфейс
   - Возможность тестирования API прямо в браузере
   - Автоматическая генерация из кода

2. **ReDoc:** `http://127.0.0.1:8000/api/redoc/`
   - Альтернативный интерфейс документации
   - Более читаемый формат

3. **Schema JSON:** `http://127.0.0.1:8000/api/schema/`
   - OpenAPI схема в формате JSON
   - Для интеграции с другими инструментами

### Особенности документации

1. **Автоматическая генерация:** Документация создается автоматически на основе кода
2. **Описания полей:** Отображаются `help_text` из сериализаторов
3. **Описания методов:** Используются `@extend_schema` декораторы
4. **Примеры:** Можно добавлять примеры запросов и ответов
5. **Тестирование:** Можно тестировать API прямо в Swagger UI

### Примеры описаний

**В сериализаторах:**
```python
extra_kwargs = {
    'title': {
        'help_text': 'Название книги (максимум 200 символов)'
    }
}
```

**В представлениях:**
```python
@extend_schema(
    summary="Получение списка книг",
    description="Возвращает список всех книг с возможностью фильтрации.",
    responses={200: BookSerializer(many=True)}
)
```

---

## 💡 Примеры использования

### 1. Получение списка категорий

**Запрос:**
```http
GET /api/categories/
```

**Ответ:**
```json
[
    {
        "id": 1,
        "name": "Фантастика",
        "description": "Книги жанра фантастика",
        "books_count": 5
    },
    {
        "id": 2,
        "name": "Детективы",
        "description": "Детективные романы",
        "books_count": 3
    }
]
```

### 2. Получение категории с вложенными книгами

**Запрос:**
```http
GET /api/categories/1/detail/
```

**Ответ:**
```json
{
    "id": 1,
    "name": "Фантастика",
    "description": "Книги жанра фантастика",
    "books": [
        {
            "id": 1,
            "title": "Дюна",
            "author": "Фрэнк Герберт",
            "published_date": "1965-08-01",
            "description": "...",
            "publisher": 1,
            "publisher_name": "Эксмо",
            "category": 1,
            "category_name": "Фантастика",
            "reviews_count": 10,
            "avg_rating": 4.8
        },
        {
            "id": 2,
            "title": "1984",
            "author": "Джордж Оруэлл",
            "published_date": "1949-06-08",
            "description": "...",
            "publisher": 2,
            "publisher_name": "АСТ",
            "category": 1,
            "category_name": "Фантастика",
            "reviews_count": 15,
            "avg_rating": 4.9
        }
    ]
}
```

### 3. Создание книги с категорией

**Запрос:**
```http
POST /api/books/
Content-Type: application/json

{
    "title": "Новая книга",
    "author": "Автор",
    "published_date": "2025-01-27",
    "description": "Описание книги",
    "publisher": 1,
    "category": 1,
    "store_ids": [1, 2]
}
```

**Ответ:**
```json
{
    "id": 10,
    "title": "Новая книга",
    "author": "Автор",
    "published_date": "2025-01-27",
    "description": "Описание книги",
    "publisher": 1,
    "publisher_name": "Эксмо",
    "category": 1,
    "category_name": "Фантастика",
    "reviews_count": 0,
    "avg_rating": null
}
```

### 4. Получение списка книг категории (кастомное действие)

**Запрос:**
```http
GET /api/categories/1/books/
```

**Ответ:**
```json
[
    {
        "id": 1,
        "title": "Дюна",
        "author": "Фрэнк Герберт",
        "category": 1,
        "category_name": "Фантастика",
        "reviews_count": 10,
        "avg_rating": 4.8
    }
]
```

### 5. Создание статьи (демо-API)

**Запрос:**
```http
POST /api/demo/articles/
Content-Type: application/json

{
    "title": "Введение в Django REST Framework",
    "content": "Django REST Framework - это мощный инструмент...",
    "author": "Иван Иванов",
    "is_published": true
}
```

**Ответ:**
```json
{
    "id": 1,
    "title": "Введение в Django REST Framework",
    "content": "Django REST Framework - это мощный инструмент...",
    "author": "Иван Иванов",
    "created_at": "2025-01-27T10:00:00Z",
    "updated_at": "2025-01-27T10:00:00Z",
    "is_published": true
}
```

---

## ✅ Выводы

### Что было сделано

1. ✅ Переход на `drf-spectacular` для современной документации
2. ✅ Добавление модели `Category` и связи с моделью `Book`
3. ✅ Реализация вложенных сериализаторов (`CategoryDetailSerializer`)
4. ✅ Улучшение сериализаторов с `help_text` для всех полей
5. ✅ Добавление подробных описаний через `@extend_schema`
6. ✅ Создание `CategoryViewSet` и `CategoryDetailView`
7. ✅ Создание демо-приложения с APIView классами
8. ✅ Демонстрация обоих подходов к созданию API

### Преимущества реализации

1. **Улучшенная документация:**
   - Автоматическая генерация из кода
   - Подробные описания методов и полей
   - Интерактивное тестирование

2. **Вложенные сериализаторы:**
   - Получение связанных данных одним запросом
   - Оптимизация через `prefetch_related`
   - Удобный формат для фронтенда

3. **Два подхода:**
   - ViewSets для полнофункционального API
   - APIView классы для простых случаев

4. **Соответствие теме:**
   - Использование `ListCreateAPIView` и `RetrieveUpdateDestroyAPIView`
   - Настройка сериализаторов с `extra_kwargs`
   - Реализация вложенных сериализаторов
   - Работа с документацией Swagger

### Рекомендации

1. **Для стандартного CRUD:** Использовать ViewSets
2. **Для вложенных данных:** Использовать вложенные сериализаторы
3. **Для обучения:** Начать с APIView классов
4. **Для документации:** Всегда добавлять `help_text` и `@extend_schema`
5. **Для тестирования:** Использовать Swagger UI

### Дальнейшее развитие

1. Добавить фильтрацию книг по категориям
2. Реализовать пагинацию для APIView классов
3. Добавить примеры аутентификации
4. Расширить документацию примерами запросов
5. Добавить валидацию для категорий

---

## 📝 Структура проекта

```
myshop/
├── books/
│   ├── models.py              # Модели Book, Category, Publisher, Store, Review
│   ├── admin.py               # Регистрация Category в админке
│   ├── migrations/
│   │   └── 0003_category_book_category.py  # Миграция для Category
│   └── api/
│       ├── serializers.py     # Сериализаторы с help_text + Category сериализаторы
│       ├── views.py           # ViewSets с @extend_schema + CategoryViewSet
│       └── urls.py             # Маршруты для Category
│
├── api_demo/                  # Демо-приложение
│   ├── models.py             # Модель Article
│   ├── serializers.py        # ArticleSerializer
│   ├── views.py              # APIView классы
│   └── urls.py               # Явные маршруты
│
├── myshop/
│   ├── settings.py           # Настройки drf-spectacular
│   └── urls.py               # Маршруты Swagger
│
└── requirements.txt          # Обновлены зависимости
```

---

## 🔗 Полезные ссылки

- **Swagger UI:** `http://127.0.0.1:8000/api/docs/`
- **ReDoc:** `http://127.0.0.1:8000/api/redoc/`
- **API Books:** `http://127.0.0.1:8000/api/books/`
- **API Categories:** `http://127.0.0.1:8000/api/categories/`
- **API Category Detail:** `http://127.0.0.1:8000/api/categories/1/detail/`
- **API Demo:** `http://127.0.0.1:8000/api/demo/articles/`

---

**Дата создания:** 2025-01-27  
**Версия:** 2.0
