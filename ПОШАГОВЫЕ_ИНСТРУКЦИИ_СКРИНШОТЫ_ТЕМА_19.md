# Пошаговые инструкции для создания скриншотов ДЗ "Тема #19. Начало работы с Моделями"

## Подготовка

Перед началом создания скриншотов убедитесь, что:
- Сервер Django запущен и работает
- База данных создана и миграции применены
- Вы вошли в админ-панель как суперпользователь
- В проекте созданы тестовые данные (категории, товары, заказы, отзывы)

---

## 1. МОДЕЛЬ CATEGORY (Категория товаров)

### Скриншот 1.1: Код модели Category

**Путь к файлу:** `shop/models.py`

**Действия:**
1. Откройте файл `shop/models.py` в редакторе кода (VS Code, PyCharm и т.д.)
2. Найдите класс `Category` (начинается примерно со строки 12)
3. Сделайте скриншот всего класса `Category` включая:
   - Определение класса с docstring
   - Поле `name` (CharField)
   - Поле `description` (TextField с blank=True, null=True)
   - Поле `parent` (ForeignKey на 'self' для вложенных категорий)
   - Класс Meta с verbose_name, verbose_name_plural, ordering
   - Метод `__str__`

**Что должно быть видно:**
```python
class Category(models.Model):
    name = models.CharField(...)
    description = models.TextField(...)
    parent = models.ForeignKey('self', ...)
    class Meta:
        verbose_name = 'Категория'
        verbose_name_plural = 'Категории'
        ordering = ['name']
    def __str__(self): ...
```

**Важно:** Убедитесь, что видно self-referencing ForeignKey (`'self'`) в поле `parent`.

---

### Скриншот 1.2: Админ-панель - список категорий

**URL:** http://127.0.0.1:8000/admin/shop/category/

**Действия:**
1. Откройте браузер
2. Перейдите по адресу: http://127.0.0.1:8000/admin/
3. Войдите с учетными данными суперпользователя
4. В левом меню найдите раздел "SHOP"
5. Нажмите на "Categories" (Категории)
6. Сделайте скриншот страницы со списком категорий

**Что должно быть видно:**
- URL в адресной строке: `/admin/shop/category/`
- Заголовок "Select category to change"
- Таблица с колонками: Name, Parent, Products count
- Кнопка "Add category" в правом верхнем углу
- Хотя бы несколько созданных категорий (включая вложенные, если есть)

---

### Скриншот 1.3: Админ-панель - форма добавления категории

**URL:** http://127.0.0.1:8000/admin/shop/category/add/

**Действия:**
1. На странице списка категорий нажмите кнопку "Add category" (Добавить категорию)
2. Сделайте скриншот формы добавления категории

**Что должно быть видно:**
- URL в адресной строке: `/admin/shop/category/add/`
- Поля формы:
  - Name (Название категории)
  - Description (Описание категории)
  - Parent (Родительская категория) - выпадающий список с существующими категориями
- Кнопки "Save", "Save and add another", "Save and continue editing"

---

## 2. МОДЕЛЬ PRODUCT (Товар) - РАСШИРЕННАЯ

### Скриншот 2.1: Код модели Product (полный класс)

**Путь к файлу:** `shop/models.py`

**Действия:**
1. Откройте файл `shop/models.py`
2. Найдите класс `Product` (начинается примерно со строки 58)
3. Сделайте скриншот всего класса `Product` включая:
   - Все поля: name, description, price, stock, image, category, created_at
   - Класс Meta с db_table, unique_together, ordering, verbose_name
   - Метод `__str__`

**Что должно быть видно:**
```python
class Product(models.Model):
    name = models.CharField(...)
    description = models.TextField(...)
    price = models.DecimalField(...)
    stock = models.IntegerField(...)  # НОВОЕ ПОЛЕ
    image = models.ImageField(...)
    category = models.ForeignKey(Category, ...)  # НОВОЕ ПОЛЕ
    created_at = models.DateTimeField(...)
    class Meta:
        db_table = 'shop_products'  # КАСТОМНОЕ ИМЯ ТАБЛИЦЫ
        unique_together = ['name', 'category']  # УНИКАЛЬНОСТЬ
        ordering = ['-created_at']
        verbose_name = 'Товар'
        verbose_name_plural = 'Товары'
```

**Важно:** Убедитесь, что видны новые поля `stock` и `category`, а также настройки Meta класса.

---

### Скриншот 2.2: Админ-панель - список товаров с новыми полями

**URL:** http://127.0.0.1:8000/admin/shop/product/

**Действия:**
1. Перейдите в админ-панель: http://127.0.0.1:8000/admin/
2. В разделе "SHOP" нажмите на "Products" (Товары)
3. Сделайте скриншот страницы со списком товаров

**Что должно быть видно:**
- URL в адресной строке: `/admin/shop/product/`
- Таблица с колонками: Name, Category, Price, Stock, Created at, Has image
- Фильтры справа: по Category и Created at
- Поисковая строка вверху
- Кнопка "Add product" в правом верхнем углу

---

### Скриншот 2.3: Админ-панель - форма редактирования товара

**URL:** http://127.0.0.1:8000/admin/shop/product/[id]/change/

**Действия:**
1. На странице списка товаров нажмите на любой товар для редактирования
2. Сделайте скриншот формы редактирования товара

**Что должно быть видно:**
- URL в адресной строке: `/admin/shop/product/[id]/change/`
- Поля формы, сгруппированные в fieldsets:
  - **Основная информация:** Name, Description, Category, Price, Stock
  - **Изображение:** Image
  - **Метаданные:** Created at (только для чтения)
- Выпадающий список "Category" с существующими категориями
- Поле "Stock" с числовым значением

---

## 3. МОДЕЛЬ ORDER (Заказ)

### Скриншот 3.1: Код модели Order

**Путь к файлу:** `shop/models.py`

**Действия:**
1. Откройте файл `shop/models.py`
2. Найдите класс `Order` (начинается примерно со строки 186)
3. Сделайте скриншот всего класса `Order` включая:
   - Константу STATUS_CHOICES с вариантами статусов
   - Поля: user, created_at, status
   - Класс Meta
   - Метод `__str__`
   - Метод `total_price()` для вычисления общей стоимости

**Что должно быть видно:**
```python
class Order(models.Model):
    STATUS_CHOICES = [
        ('processing', 'В обработке'),
        ('shipping', 'Доставляется'),
        ('delivered', 'Доставлено'),
    ]
    user = models.ForeignKey(User, ...)
    created_at = models.DateTimeField(...)
    status = models.CharField(choices=STATUS_CHOICES, ...)
    def total_price(self): ...
```

**Важно:** Убедитесь, что виден метод `total_price()` с логикой вычисления стоимости.

---

### Скриншот 3.2: Админ-панель - список заказов

**URL:** http://127.0.0.1:8000/admin/shop/order/

**Действия:**
1. Перейдите в админ-панель: http://127.0.0.1:8000/admin/
2. В разделе "SHOP" нажмите на "Orders" (Заказы)
3. Сделайте скриншот страницы со списком заказов

**Что должно быть видно:**
- URL в адресной строке: `/admin/shop/order/`
- Таблица с колонками: ID, User, Status, Total price, Created at
- Фильтры справа: по Status и Created at
- Поисковая строка
- Кнопка "Add order" в правом верхнем углу
- Хотя бы один созданный заказ

---

### Скриншот 3.3: Админ-панель - форма редактирования заказа с OrderItem

**URL:** http://127.0.0.1:8000/admin/shop/order/[id]/change/

**Действия:**
1. На странице списка заказов нажмите на любой заказ для редактирования
2. Прокрутите страницу вниз до раздела с товарами в заказе (Order items)
3. Сделайте скриншот формы редактирования заказа

**Что должно быть видно:**
- URL в адресной строке: `/admin/shop/order/[id]/change/`
- Поля заказа:
  - User (выпадающий список)
  - Status (выпадающий список с вариантами: В обработке, Доставляется, Доставлено)
  - Created at (только для чтения)
  - Total price (только для чтения, вычисляемое значение)
- Таблица "Order items" (Товары в заказе) с полями:
  - Product (выпадающий список)
  - Quantity (количество)
- Кнопки для добавления/удаления товаров в заказе

---

## 4. МОДЕЛЬ ORDERITEM (Товар в заказе)

### Скриншот 4.1: Код модели OrderItem

**Путь к файлу:** `shop/models.py`

**Действия:**
1. Откройте файл `shop/models.py`
2. Найдите класс `OrderItem` (начинается примерно со строки 249)
3. Сделайте скриншот всего класса `OrderItem` включая:
   - Поля: order, product, quantity
   - Класс Meta
   - Метод `__str__`

**Что должно быть видно:**
```python
class OrderItem(models.Model):
    order = models.ForeignKey(Order, ...)
    product = models.ForeignKey(Product, ...)
    quantity = models.IntegerField(...)
    class Meta:
        verbose_name = 'Товар в заказе'
        verbose_name_plural = 'Товары в заказах'
```

---

### Скриншот 4.2: Админ-панель - список товаров в заказах

**URL:** http://127.0.0.1:8000/admin/shop/orderitem/

**Действия:**
1. Перейдите в админ-панель: http://127.0.0.1:8000/admin/
2. В разделе "SHOP" нажмите на "Order items" (Товары в заказах)
3. Сделайте скриншот страницы со списком товаров в заказах

**Что должно быть видно:**
- URL в адресной строке: `/admin/shop/orderitem/`
- Таблица с колонками: Order, Product, Quantity, Item total
- Фильтры справа: по Order
- Поисковая строка
- Кнопка "Add order item" в правом верхнем углу

---

## 5. МОДЕЛЬ REVIEW (Отзыв)

### Скриншот 5.1: Код модели Review

**Путь к файлу:** `shop/models.py`

**Действия:**
1. Откройте файл `shop/models.py`
2. Найдите класс `Review` (начинается примерно со строки 290)
3. Сделайте скриншот всего класса `Review` включая:
   - Поля: product, user, rating, text, created_at
   - Валидацию rating с MinValueValidator(1) и MaxValueValidator(5)
   - Класс Meta
   - Метод `__str__`

**Что должно быть видно:**
```python
class Review(models.Model):
    product = models.ForeignKey(Product, ...)
    user = models.ForeignKey(User, ...)
    rating = models.IntegerField(
        validators=[MinValueValidator(1), MaxValueValidator(5)],
        ...
    )
    text = models.TextField(...)
    created_at = models.DateTimeField(...)
```

**Важно:** Убедитесь, что видны валидаторы для поля rating.

---

### Скриншот 5.2: Админ-панель - список отзывов

**URL:** http://127.0.0.1:8000/admin/shop/review/

**Действия:**
1. Перейдите в админ-панель: http://127.0.0.1:8000/admin/
2. В разделе "SHOP" нажмите на "Reviews" (Отзывы)
3. Сделайте скриншот страницы со списком отзывов

**Что должно быть видно:**
- URL в адресной строке: `/admin/shop/review/`
- Таблица с колонками: Product, User, Rating, Created at, Text preview
- Фильтры справа: по Rating и Created at
- Поисковая строка
- Кнопка "Add review" в правом верхнем углу
- Хотя бы несколько созданных отзывов с разными оценками

---

### Скриншот 5.3: Админ-панель - форма добавления отзыва

**URL:** http://127.0.0.1:8000/admin/shop/review/add/

**Действия:**
1. На странице списка отзывов нажмите кнопку "Add review" (Добавить отзыв)
2. Сделайте скриншот формы добавления отзыва

**Что должно быть видно:**
- URL в адресной строке: `/admin/shop/review/add/`
- Поля формы:
  - Product (выпадающий список с товарами)
  - User (выпадающий список с пользователями)
  - Rating (числовое поле от 1 до 5)
  - Text (текстовое поле для текста отзыва)
  - Created at (только для чтения, заполнится автоматически)
- Кнопки "Save", "Save and add another", "Save and continue editing"

---

## 6. МОДЕЛИ CART И CARTITEM (Корзина покупок) - БОНУС

### Скриншот 6.1: Код модели Cart

**Путь к файлу:** `shop/models.py`

**Действия:**
1. Откройте файл `shop/models.py`
2. Найдите класс `Cart` (начинается примерно со строки 344)
3. Сделайте скриншот всего класса `Cart` включая:
   - Поле `user` с OneToOneField
   - Поля created_at и updated_at
   - Класс Meta
   - Метод `__str__`

**Что должно быть видно:**
```python
class Cart(models.Model):
    user = models.OneToOneField(User, ...)
    created_at = models.DateTimeField(...)
    updated_at = models.DateTimeField(...)
```

**Важно:** Убедитесь, что видно использование OneToOneField вместо ForeignKey.

---

### Скриншот 6.2: Код модели CartItem

**Путь к файлу:** `shop/models.py`

**Действия:**
1. Откройте файл `shop/models.py`
2. Найдите класс `CartItem` (начинается примерно со строки 382)
3. Сделайте скриншот всего класса `CartItem` включая:
   - Поля: cart, product, quantity
   - Класс Meta с unique_together
   - Метод `__str__`

**Что должно быть видно:**
```python
class CartItem(models.Model):
    cart = models.ForeignKey(Cart, ...)
    product = models.ForeignKey(Product, ...)
    quantity = models.IntegerField(...)
    class Meta:
        unique_together = ['cart', 'product']
```

---

### Скриншот 6.3: Админ-панель - список корзин

**URL:** http://127.0.0.1:8000/admin/shop/cart/

**Действия:**
1. Перейдите в админ-панель: http://127.0.0.1:8000/admin/
2. В разделе "SHOP" нажмите на "Carts" (Корзины)
3. Сделайте скриншот страницы со списком корзин

**Что должно быть видно:**
- URL в адресной строке: `/admin/shop/cart/`
- Таблица с колонками: User, Items count, Created at, Updated at
- Поисковая строка
- Кнопка "Add cart" в правом верхнем углу

---

## 7. МЕТА-КЛАССЫ И НАСТРОЙКИ

### Скриншот 7.1: Meta класс модели Product

**Путь к файлу:** `shop/models.py`

**Действия:**
1. Откройте файл `shop/models.py`
2. Найдите класс `Product` и его внутренний класс `Meta`
3. Сделайте скриншот класса Meta с увеличенным масштабом

**Что должно быть видно:**
```python
class Meta:
    verbose_name = 'Товар'
    verbose_name_plural = 'Товары'
    ordering = ['-created_at']
    db_table = 'shop_products'
    unique_together = ['name', 'category']
```

**Важно:** Убедитесь, что видны все настройки Meta класса, особенно `db_table` и `unique_together`.

---

### Скриншот 7.2: Метод total_price() в модели Order

**Путь к файлу:** `shop/models.py`

**Действия:**
1. Откройте файл `shop/models.py`
2. Найдите класс `Order` и метод `total_price()`
3. Сделайте скриншот метода `total_price()` с полной логикой вычисления

**Что должно быть видно:**
```python
def total_price(self):
    """
    Вычисляет общую стоимость заказа.
    ...
    """
    total = Decimal('0.00')
    for item in self.items.all():
        total += item.product.price * item.quantity
    return total
```

**Важно:** Убедитесь, что видна полная логика метода с циклом по items.

---

## 8. АДМИН-ПАНЕЛЬ - ОБЩИЙ ВИД

### Скриншот 8.1: Главная страница админ-панели с разделом SHOP

**URL:** http://127.0.0.1:8000/admin/

**Действия:**
1. Откройте браузер
2. Перейдите по адресу: http://127.0.0.1:8000/admin/
3. Войдите с учетными данными суперпользователя
4. Сделайте скриншот главной страницы админ-панели

**Что должно быть видно:**
- URL в адресной строке: `/admin/`
- Левое меню с разделом "SHOP"
- В разделе "SHOP" должны быть видны все модели:
  - Categories
  - Products
  - Messages
  - Orders
  - Order items
  - Reviews
  - Carts
  - Cart items

---

### Скриншот 8.2: Структура моделей в админ-панели (развернутый раздел SHOP)

**URL:** http://127.0.0.1:8000/admin/

**Действия:**
1. На главной странице админ-панели разверните раздел "SHOP" (нажмите на него)
2. Сделайте скриншот развернутого раздела со всеми моделями

**Что должно быть видно:**
- Развернутый раздел "SHOP"
- Список всех моделей:
  - Categories (Категории)
  - Products (Товары)
  - Messages (Сообщения)
  - Orders (Заказы)
  - Order items (Товары в заказах)
  - Reviews (Отзывы)
  - Carts (Корзины)
  - Cart items (Товары в корзинах)

---

## 9. РАБОТА С DJANGO SHELL (Часть 3 ДЗ)

### Скриншот 9.1: Django Shell - создание категорий

**Действия:**
1. Откройте терминал/командную строку
2. Перейдите в директорию проекта
3. Запустите Django Shell:
   ```bash
   python manage.py shell
   ```
4. Выполните код для создания категорий:
   ```python
   from shop.models import Category
   
   electronics = Category.objects.create(
       name='Электроника',
       description='Электронные устройства'
   )
   
   smartphones = Category.objects.create(
       name='Смартфоны',
       parent=electronics
   )
   ```
5. Сделайте скриншот терминала с выполненными командами и результатами

**Что должно быть видно:**
- Команду запуска shell
- Импорт модели Category
- Команды создания категорий
- Результат выполнения (созданные объекты)

---

### Скриншот 9.2: Django Shell - создание товаров

**Действия:**
1. В Django Shell выполните код для создания товаров:
   ```python
   from shop.models import Product, Category
   
   smartphones = Category.objects.get(name='Смартфоны')
   
   product = Product.objects.create(
       name='iPhone 15 Pro',
       description='Новейший смартфон',
       price=99999.00,
       stock=15,
       category=smartphones
   )
   
   print(product)
   ```
2. Сделайте скриншот терминала с выполненными командами

**Что должно быть видно:**
- Импорт моделей Product и Category
- Получение категории
- Создание товара с полями name, description, price, stock, category
- Вывод созданного товара

---

### Скриншот 9.3: Django Shell - запрос: товары из категории

**Действия:**
1. В Django Shell выполните запрос:
   ```python
   from shop.models import Product, Category
   
   smartphones = Category.objects.get(name='Смартфоны')
   products = Product.objects.filter(category=smartphones)
   
   for product in products:
       print(f"{product.name} - {product.price} руб. - В наличии: {product.stock}")
   ```
2. Сделайте скриншот терминала с результатами запроса

**Что должно быть видно:**
- Запрос filter по категории
- Цикл по результатам
- Вывод информации о товарах: название, цена, количество на складе

---

### Скриншот 9.4: Django Shell - запрос: заказы пользователя

**Действия:**
1. В Django Shell выполните запрос:
   ```python
   from django.contrib.auth.models import User
   from shop.models import Order
   
   user = User.objects.first()
   orders = Order.objects.filter(user=user)
   
   for order in orders:
       print(f"Заказ #{order.id} - {order.get_status_display()} - {order.total_price()} руб.")
   ```
2. Сделайте скриншот терминала с результатами

**Что должно быть видно:**
- Получение пользователя
- Запрос заказов пользователя
- Цикл по заказам
- Вывод: номер заказа, статус, общая стоимость (через метод total_price())

---

### Скриншот 9.5: Django Shell - запрос: отзывы для товара

**Действия:**
1. В Django Shell выполните запрос:
   ```python
   from shop.models import Review, Product
   
   product = Product.objects.first()
   reviews = Review.objects.filter(product=product)
   
   for review in reviews:
       print(f"{review.user.username}: {review.rating}/5 - {review.text[:50]}...")
   ```
2. Сделайте скриншот терминала с результатами

**Что должно быть видно:**
- Получение товара
- Запрос отзывов для товара
- Цикл по отзывам
- Вывод: имя пользователя, оценка, краткий текст отзыва

---

### Скриншот 9.6: Django Shell - запрос: товары со складом > 10

**Действия:**
1. В Django Shell выполните запрос:
   ```python
   from shop.models import Product
   
   products_in_stock = Product.objects.filter(stock__gt=10)
   
   print(f"Товаров на складе больше 10: {products_in_stock.count()}")
   for product in products_in_stock:
       print(f"{product.name} - В наличии: {product.stock} шт.")
   ```
2. Сделайте скриншот терминала с результатами

**Что должно быть видно:**
- Запрос filter с условием stock__gt=10
- Подсчет количества товаров
- Вывод списка товаров с количеством на складе

---

## 10. МИГРАЦИИ

### Скриншот 10.1: Список миграций приложения shop

**Действия:**
1. Откройте терминал/командную строку
2. Перейдите в директорию проекта
3. Выполните команду:
   ```bash
   python manage.py showmigrations shop
   ```
4. Сделайте скриншот вывода команды

**Что должно быть видно:**
- Список миграций приложения shop
- `[X] 0001_initial` (отмечена как примененная)
- `[X] 0002_message` (отмечена как примененная)
- `[X] 0003_cart_order_product_stock_alter_message_message_and_more` (отмечена как примененная)

---

### Скриншот 10.2: Файл миграции 0003

**Путь к файлу:** `shop/migrations/0003_cart_order_product_stock_alter_message_message_and_more.py`

**Действия:**
1. Откройте файл миграции в редакторе кода
2. Сделайте скриншот начала файла с классом Migration и операциями

**Что должно быть видно:**
- Класс Migration с dependencies
- Список операций migrations:
  - CreateModel для новых моделей
  - AddField для новых полей
  - AlterField для измененных полей
  - RenameModel для переименования таблицы

---

## 11. СТРУКТУРА ФАЙЛОВ

### Скриншот 11.1: Структура директории shop

**Действия:**
1. Откройте проводник Windows (или файловый менеджер)
2. Перейдите в папку проекта
3. Откройте папку `shop/`
4. Сделайте скриншот структуры папки shop

**Что должно быть видно:**
- Файлы:
  - `__init__.py`
  - `admin.py`
  - `apps.py`
  - `forms.py`
  - `models.py`
  - `urls.py`
  - `views.py`
- Папка `migrations/` с файлами миграций

---

### Скриншот 11.2: Содержимое папки migrations

**Действия:**
1. Откройте папку `shop/migrations/`
2. Сделайте скриншот содержимого папки

**Что должно быть видно:**
- Файлы миграций:
  - `__init__.py`
  - `0001_initial.py`
  - `0002_message.py`
  - `0003_cart_order_product_stock_alter_message_message_and_more.py`

---

## 12. ДОПОЛНИТЕЛЬНЫЕ СКРИНШОТЫ

### Скриншот 12.1: Админ-панель - фильтрация товаров по категории

**URL:** http://127.0.0.1:8000/admin/shop/product/?category__id__exact=[id]

**Действия:**
1. Перейдите в админ-панель: http://127.0.0.1:8000/admin/shop/product/
2. В правой панели фильтров нажмите на категорию для фильтрации
3. Сделайте скриншот отфильтрованного списка товаров

**Что должно быть видно:**
- URL с параметром фильтрации
- Отфильтрованный список товаров только из выбранной категории
- Активный фильтр в правой панели

---

### Скриншот 12.2: Админ-панель - заказ с вычисленной общей стоимостью

**URL:** http://127.0.0.1:8000/admin/shop/order/[id]/change/

**Действия:**
1. Откройте любой заказ с добавленными товарами
2. Прокрутите до поля "Total price"
3. Сделайте скриншот, показывающий вычисленное значение общей стоимости

**Что должно быть видно:**
- Поле "Total price" с вычисленным значением (например, "99999.00 руб.")
- Поле помечено как "readonly" (только для чтения)
- Таблица Order items с товарами и их количеством

---

### Скриншот 12.3: Админ-панель - уникальность названия товара в категории

**Действия:**
1. Перейдите в админ-панель: http://127.0.0.1:8000/admin/shop/product/add/
2. Попробуйте создать товар с названием, которое уже существует в той же категории
3. Сделайте скриншот ошибки валидации

**Что должно быть видно:**
- Форма добавления товара
- Сообщение об ошибке валидации (красное поле или сообщение об ошибке)
- Текст ошибки, указывающий на нарушение unique_together

---

## ИТОГОВЫЙ ЧЕКЛИСТ

Перед сдачей проверьте, что у вас есть скриншоты:

### Обязательные (минимум):
- [ ] Код модели Category с self-referencing ForeignKey
- [ ] Код модели Product с полями category и stock
- [ ] Код модели Product с Meta классом (db_table, unique_together)
- [ ] Код модели Order с методом total_price()
- [ ] Код модели OrderItem
- [ ] Код модели Review с валидацией rating
- [ ] Код моделей Cart и CartItem (бонус)
- [ ] Админ-панель - список всех моделей в разделе SHOP
- [ ] Админ-панель - форма редактирования товара с новыми полями
- [ ] Админ-панель - форма редактирования заказа с OrderItem
- [ ] Django Shell - создание категорий и товаров
- [ ] Django Shell - запрос: товары из категории
- [ ] Django Shell - запрос: заказы пользователя
- [ ] Django Shell - запрос: отзывы для товара
- [ ] Django Shell - запрос: товары со складом > 10
- [ ] Миграции - список примененных миграций

### Дополнительные (для полноты):
- [ ] Админ-панель - форма добавления категории
- [ ] Админ-панель - форма добавления отзыва
- [ ] Админ-панель - список корзин
- [ ] Админ-панель - фильтрация товаров по категории
- [ ] Админ-панель - вычисленная общая стоимость заказа
- [ ] Админ-панель - ошибка валидации unique_together
- [ ] Структура файлов проекта
- [ ] Файл миграции 0003

---

## СОВЕТЫ ПО СЪЕМКЕ СКРИНШОТОВ

1. **Используйте одинаковое разрешение** для всех скриншотов интерфейса
2. **Показывайте URL** в адресной строке браузера для скриншотов веб-страниц
3. **Делайте скриншоты кода с подсветкой синтаксиса** (VS Code, PyCharm)
4. **Используйте полноэкранный режим** для скриншотов страниц админ-панели
5. **Группируйте скриншоты** в документе по темам (модели, админ-панель, Django Shell)
6. **Добавляйте подписи** к каждому скриншоту с указанием, что на нем показано
7. **Для скриншотов Django Shell** убедитесь, что виден весь вывод команды
8. **Для скриншотов админ-панели** показывайте активные фильтры и поисковые запросы

---

## БЫСТРЫЙ СПИСОК URL ДЛЯ ПРОВЕРКИ

### Админ-панель:
- http://127.0.0.1:8000/admin/ - Главная страница
- http://127.0.0.1:8000/admin/shop/category/ - Категории
- http://127.0.0.1:8000/admin/shop/product/ - Товары
- http://127.0.0.1:8000/admin/shop/order/ - Заказы
- http://127.0.0.1:8000/admin/shop/orderitem/ - Товары в заказах
- http://127.0.0.1:8000/admin/shop/review/ - Отзывы
- http://127.0.0.1:8000/admin/shop/cart/ - Корзины
- http://127.0.0.1:8000/admin/shop/cartitem/ - Товары в корзинах

### Формы добавления:
- http://127.0.0.1:8000/admin/shop/category/add/
- http://127.0.0.1:8000/admin/shop/product/add/
- http://127.0.0.1:8000/admin/shop/order/add/
- http://127.0.0.1:8000/admin/shop/review/add/

---

## ВАЖНЫЕ ЗАМЕЧАНИЯ

1. **Перед созданием скриншотов** убедитесь, что в базе данных есть тестовые данные:
   - Хотя бы 2-3 категории (включая вложенные)
   - Хотя бы 5-10 товаров в разных категориях
   - Хотя бы 2-3 заказа с товарами
   - Хотя бы 3-5 отзывов на разные товары

2. **Для скриншотов Django Shell** используйте реальные данные из базы, а не только создание новых объектов

3. **Показывайте результаты запросов**, а не только код запросов

4. **Для скриншотов админ-панели** убедитесь, что видны все важные элементы интерфейса

5. **Проверьте уникальность** - покажите, что unique_together работает корректно

---

Удачи с созданием скриншотов! 🎯

