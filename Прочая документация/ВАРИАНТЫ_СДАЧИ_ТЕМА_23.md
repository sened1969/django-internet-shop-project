# Варианты сдачи задания "Тема #23. REST API"

**Дата создания:** 2025-01-27

---

## 🎯 Краткое описание вариантов

### ✅ Вариант 1: Полная интеграция REST API в приложение `books` (РЕКОМЕНДУЕТСЯ)

**Что реализовать:**
- REST API для всех моделей: Book, Publisher, Store, Review
- Система ролей (расширение CustomUser в shop)
- Ролевые разрешения для API:
  - Администратор: полный доступ (CRUD)
  - Менеджер: создание и редактирование (без удаления)
  - Пользователь: только чтение
- Аутентификация через сессии и JWT токены
- Swagger документация

**Преимущества:**
- ✅ Максимальная интеграция с проектом
- ✅ Использование существующих моделей
- ✅ Демонстрация работы с различными типами связей
- ✅ Соответствие структуре проекта
- ✅ Практическая применимость

**Недостатки:**
- ⚠️ Не полностью соответствует заданию (в задании нужны проекты, а не книги)

**Сложность:** ⭐⭐⭐⭐⭐ (5/5)

**Структура:**
```
books/
├── api/
│   ├── __init__.py
│   ├── serializers.py      # BookSerializer, PublisherSerializer, StoreSerializer, ReviewSerializer
│   ├── views.py           # ViewSets для всех моделей
│   ├── permissions.py     # IsAdminOrReadOnly, IsManagerOrReadOnly
│   └── urls.py            # API маршруты
├── models.py (без изменений)
└── views.py (без изменений - веб-интерфейс остается)

shop/
└── models.py (добавить поле role в CustomUser)
```

---

### ⚡ Вариант 2: Отдельное приложение `projects` для задания

**Что реализовать:**
- Новое приложение `projects` с моделями:
  - Project (проект)
  - Task (задача)
- Система ролей для управления проектами
- REST API для проектов и задач
- Ролевые разрешения согласно заданию:
  - **Администратор**: полный доступ (CRUD проектов, управление пользователями)
  - **Менеджер**: просмотр проектов, создание/редактирование задач, назначение задач
  - **Пользователь**: просмотр проектов, добавление комментариев, отметка задач как завершенных

**Преимущества:**
- ✅ Точное соответствие заданию
- ✅ Независимое приложение
- ✅ Не затрагивает существующий код
- ✅ Четкая структура для демонстрации навыков

**Недостатки:**
- ❌ Дополнительные модели
- ❌ Меньшая интеграция с проектом

**Сложность:** ⭐⭐⭐⭐ (4/5)

**Структура:**
```
projects/
├── models.py          # Project, Task, Comment (опционально)
├── serializers.py     # ProjectSerializer, TaskSerializer
├── views.py           # ViewSets для проектов и задач
├── permissions.py     # Ролевые разрешения
├── urls.py            # API маршруты
├── admin.py           # Регистрация в админке
└── migrations/
```

**Модели:**
```python
class Project(models.Model):
    name = models.CharField(max_length=200)
    description = models.TextField()
    created_by = models.ForeignKey(CustomUser, on_delete=models.CASCADE)
    created_at = models.DateTimeField(auto_now_add=True)

class Task(models.Model):
    project = models.ForeignKey(Project, on_delete=models.CASCADE, related_name='tasks')
    title = models.CharField(max_length=200)
    description = models.TextField()
    assigned_to = models.ForeignKey(CustomUser, on_delete=models.SET_NULL, null=True)
    status = models.CharField(max_length=20, choices=[...])
    created_at = models.DateTimeField(auto_now_add=True)
```

---

### 🛒 Вариант 3: Расширение приложения `shop` с REST API

**Что реализовать:**
- REST API для моделей Product, Order, Review
- Система ролей для управления магазином
- Ролевые разрешения:
  - **Администратор**: полный доступ ко всем ресурсам
  - **Менеджер**: управление товарами и заказами (без удаления пользователей)
  - **Пользователь**: просмотр товаров, создание заказов, оставление отзывов

**Преимущества:**
- ✅ Использование существующих моделей
- ✅ Практическая применимость
- ✅ Расширение функциональности магазина
- ✅ Демонстрация работы с заказами и корзиной

**Недостатки:**
- ❌ Не соответствует заданию (нужны проекты, а не товары)
- ❌ Может быть сложнее из-за связей с корзиной

**Сложность:** ⭐⭐⭐⭐ (4/5)

---

### 📚 Вариант 4: Комбинированный подход

**Что реализовать:**
- REST API для `books` (основное задание)
- Система ролей в `shop` (расширение)
- Демонстрация работы с обоими приложениями
- Единая система аутентификации

**Преимущества:**
- ✅ Максимальная демонстрация навыков
- ✅ Работа с разными типами моделей
- ✅ Полноценная интеграция
- ✅ Показывает понимание архитектуры

**Недостатки:**
- ❌ Больше работы
- ❌ Может быть избыточно для задания
- ❌ Сложнее для проверки

**Сложность:** ⭐⭐⭐⭐⭐ (5/5)

---

## 📋 Структура сдачи (для всех вариантов)

### 1. GitHub репозиторий
- Выделенный коммит: `Тема_23_REST_API`
- Все файлы закоммичены
- Обновленный `requirements.txt` с:
  - `djangorestframework`
  - `djangorestframework-simplejwt`
  - `drf-yasg` (опционально, для Swagger)

### 2. Документация (на выбор):

**Вариант A: Google Colab ноутбук** (как в теме 20)
- Markdown с описанием
- Код с примерами
- Встроенные скриншоты Postman/Swagger
- Примеры запросов и ответов

**Вариант B: Markdown файл в репозитории**
- `ОТЧЕТ_ТЕМА_23.md` в корне проекта
- Структурированное описание
- Ссылки на скриншоты
- Примеры кода

**Вариант C: Комбинация**
- GitHub + Markdown + Colab
- Swagger документация (интерактивная)

### 3. Обязательные разделы документации:

#### 3.1. Описание системы ролей
- Как реализована модель ролей
- Какие роли существуют
- Права каждой роли
- Как назначаются роли пользователям
- Примеры использования

#### 3.2. Описание аутентификации
- Сессионная аутентификация (для веб-интерфейса)
- JWT токены (для REST API)
- Как получить токен
- Как использовать токен в запросах
- Примеры запросов с токенами

#### 3.3. Описание REST API эндпоинтов
Для каждого ресурса:
- URL эндпоинта
- HTTP методы (GET, POST, PUT, PATCH, DELETE)
- Параметры запроса
- Тело запроса (для POST/PUT/PATCH)
- Примеры ответов (успешных и ошибок)
- Коды статуса
- Требования к аутентификации и авторизации

**Пример структуры:**
```
## API эндпоинты

### Books (Книги)

#### GET /api/books/
Получение списка всех книг

**Аутентификация:** Не требуется (или требуется в зависимости от настроек)

**Параметры запроса:**
- `page` (int): номер страницы для пагинации
- `page_size` (int): количество элементов на странице

**Пример запроса:**
```bash
GET /api/books/?page=1&page_size=10
```

**Пример ответа (200 OK):**
```json
{
  "count": 50,
  "next": "http://localhost:8000/api/books/?page=2",
  "previous": null,
  "results": [
    {
      "id": 1,
      "title": "Название книги",
      "author": "Автор",
      "publisher": {
        "id": 1,
        "name": "Издательство"
      },
      "published_date": "2024-01-01"
    }
  ]
}
```

#### POST /api/books/
Создание новой книги

**Аутентификация:** Требуется (JWT токен)
**Авторизация:** Администратор или Менеджер

**Тело запроса:**
```json
{
  "title": "Название книги",
  "author": "Автор",
  "published_date": "2024-01-01",
  "description": "Описание книги",
  "publisher": 1,
  "stores": [1, 2]
}
```

**Пример ответа (201 Created):**
```json
{
  "id": 1,
  "title": "Название книги",
  "author": "Автор",
  ...
}
```

**Пример ошибки (403 Forbidden):**
```json
{
  "detail": "You do not have permission to perform this action."
}
```
```

#### 3.4. Описание разрешений (Permissions)
- Какие разрешения реализованы
- Как они работают
- Примеры для каждой роли
- Тесты доступа для каждой роли

**Пример:**
```
## Система разрешений

### IsAdminOrReadOnly
- Разрешает чтение всем пользователям
- Разрешает изменение только администраторам

### IsManagerOrReadOnly
- Разрешает чтение всем пользователям
- Разрешает создание и редактирование менеджерам и администраторам
- Разрешает удаление только администраторам

### Тестирование разрешений

#### Тест 1: Пользователь пытается создать книгу
**Роль:** Пользователь
**Запрос:** POST /api/books/
**Ожидаемый результат:** 403 Forbidden

#### Тест 2: Менеджер создает книгу
**Роль:** Менеджер
**Запрос:** POST /api/books/
**Ожидаемый результат:** 201 Created

#### Тест 3: Менеджер пытается удалить книгу
**Роль:** Менеджер
**Запрос:** DELETE /api/books/1/
**Ожидаемый результат:** 403 Forbidden
```

#### 3.5. Скриншоты
- Postman запросы (с токенами в заголовках)
- Swagger документация (если настроена)
- Примеры успешных ответов
- Примеры ошибок (401, 403, 404, 400)
- Админка с ролями пользователей

**Обязательные скриншоты:**
1. Получение JWT токена (POST /api/token/)
2. Запрос с токеном в заголовке Authorization
3. Список книг (GET /api/books/)
4. Создание книги администратором (POST /api/books/)
5. Ошибка доступа пользователя (403 Forbidden)
6. Swagger документация (если настроена)
7. Админка с пользователями и их ролями

#### 3.6. Тестирование
- Описание тестов для каждой роли
- Проверка доступа к ресурсам
- Проверка аутентификации
- Примеры тестовых сценариев

**Пример:**
```
## Тестовые сценарии

### Сценарий 1: Регистрация и получение роли
1. Пользователь регистрируется через POST /api/auth/register/
2. По умолчанию получает роль "Пользователь"
3. Администратор может изменить роль через админку

### Сценарий 2: Аутентификация и получение токена
1. Пользователь отправляет POST /api/token/ с email и паролем
2. Получает access_token и refresh_token
3. Использует access_token в заголовке Authorization: Bearer <token>

### Сценарий 3: Доступ к ресурсам в зависимости от роли
1. Пользователь (role='user') может только читать (GET)
2. Менеджер (role='manager') может читать и создавать (GET, POST)
3. Администратор (role='admin') может все (GET, POST, PUT, DELETE)
```

---

## ✅ Чек-лист готовности

### Функциональность
- [ ] Система ролей реализована
- [ ] Регистрация с выбором роли (или назначение по умолчанию)
- [ ] Аутентификация через сессии работает
- [ ] JWT токены работают
- [ ] REST API эндпоинты реализованы для всех ресурсов
- [ ] Ролевые разрешения работают корректно
- [ ] Обработка ошибок настроена
- [ ] Пагинация настроена

### Код
- [ ] Django REST Framework установлен и настроен
- [ ] Сериализаторы созданы для всех моделей
- [ ] ViewSets/APIViews созданы
- [ ] Permissions настроены
- [ ] URL маршруты настроены
- [ ] Код документирован (docstrings)
- [ ] Миграции созданы и применены

### Документация
- [ ] Описание системы ролей
- [ ] Описание аутентификации (сессии + JWT)
- [ ] Описание всех API эндпоинтов
- [ ] Примеры запросов Postman
- [ ] Скриншоты Swagger (если настроена)
- [ ] Инструкции по использованию
- [ ] Описание разрешений и тестов

### GitHub
- [ ] Коммит с меткой "Тема_23_REST_API"
- [ ] Все файлы закоммичены
- [ ] requirements.txt обновлен
- [ ] README.md обновлен (опционально)

### Тестирование
- [ ] Протестированы все эндпоинты
- [ ] Протестированы все роли
- [ ] Протестирована аутентификация
- [ ] Протестированы ошибки доступа
- [ ] Создана коллекция Postman (опционально)

---

## 🎯 Рекомендация

**Выбрать Вариант 1** - полная интеграция REST API в приложение `books`

**Обоснование:**
- ✅ Максимальная интеграция с проектом
- ✅ Использование существующих моделей
- ✅ Демонстрация работы с различными типами связей
- ✅ Соответствие структуре проекта
- ✅ Практическая применимость
- ✅ Можно адаптировать роли под задание (Администратор, Менеджер, Пользователь)

**Альтернатива:** Если куратор настаивает на точном соответствии заданию (система управления проектами), выбрать **Вариант 2** - отдельное приложение `projects`.

---

## 📝 Дополнительные рекомендации

### Для варианта 1 (books):

**Адаптация ролей под задание:**
- Администратор: полный CRUD для всех моделей
- Менеджер: может создавать и редактировать книги, издательства, магазины, но не удалять
- Пользователь: может только читать и создавать отзывы

**Примеры эндпоинтов:**
```
GET    /api/books/              # Все роли
POST   /api/books/              # Администратор, Менеджер
GET    /api/books/{id}/         # Все роли
PUT    /api/books/{id}/         # Администратор, Менеджер
DELETE /api/books/{id}/         # Только Администратор

GET    /api/reviews/            # Все роли
POST   /api/reviews/            # Все роли (авторизованные)
```

### Для варианта 2 (projects):

**Модели:**
- Project (проект)
- Task (задача)
- Comment (комментарий к задаче) - опционально

**Роли:**
- Администратор: полный доступ
- Менеджер: управление задачами, назначение задач
- Пользователь: просмотр, комментарии, отметка задач

---

## 🔧 Технические детали

### Установка зависимостей

```bash
pip install djangorestframework
pip install djangorestframework-simplejwt
pip install drf-yasg  # Опционально, для Swagger
```

### Настройка settings.py

```python
INSTALLED_APPS = [
    # ... существующие приложения
    'rest_framework',
    'rest_framework_simplejwt',
    'drf_yasg',  # Опционально
]

REST_FRAMEWORK = {
    'DEFAULT_AUTHENTICATION_CLASSES': [
        'rest_framework.authentication.SessionAuthentication',
        'rest_framework_simplejwt.authentication.JWTAuthentication',
    ],
    'DEFAULT_PERMISSION_CLASSES': [
        'rest_framework.permissions.IsAuthenticated',
    ],
    'DEFAULT_PAGINATION_CLASS': 'rest_framework.pagination.PageNumberPagination',
    'PAGE_SIZE': 20,
}
```

### Пример структуры URL

```python
# myshop/urls.py
urlpatterns = [
    path('admin/', admin.site.urls),
    path('', include('shop.urls')),
    path('books/', include('books.urls')),
    path('api/', include('books.api.urls')),  # REST API
    path('api/auth/', include('rest_framework.urls')),  # Сессии
    path('api/token/', TokenObtainPairView.as_view()),  # JWT
    path('api/token/refresh/', TokenRefreshView.as_view()),
    path('swagger/', schema_view.with_ui('swagger')),  # Опционально
]
```

---

**Дата создания:** 2025-01-27  
**Версия:** 1.0

