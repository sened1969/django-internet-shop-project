# Пошаговые инструкции для создания скриншотов ДЗ (3)

## Подготовка

1. **Убедитесь, что сервер запущен:**
   ```bash
   python manage.py runserver
   ```

2. **Откройте браузер:** Chrome, Firefox или Edge

3. **Подготовьте тестовые данные:**
   - Создайте хотя бы одного пользователя через регистрацию
   - Отправьте хотя бы одно сообщение через форму

---

## 1. МОДЕЛЬ ПОЛЬЗОВАТЕЛЯ И MESSAGE

### Скриншот 1.1: Код модели Message

**Путь к файлу:** `shop/models.py`

**Действия:**
1. Откройте файл `shop/models.py` в редакторе кода (VS Code, PyCharm и т.д.)
2. Найдите класс `Message` (начинается примерно со строки 67)
3. Сделайте скриншот всего класса `Message` включая:
   - Определение класса
   - Все поля (user, name, email, message, created_at)
   - Класс Meta
   - Метод `__str__`

**Что должно быть видно:**
```python
class Message(models.Model):
    user = models.ForeignKey(...)
    name = models.CharField(...)
    email = models.EmailField(...)
    message = models.TextField(...)
    created_at = models.DateTimeField(...)
    ...
```

---

### Скриншот 1.2: Админ-панель с моделью Message

**URL:** http://127.0.0.1:8000/admin/shop/message/

**Действия:**
1. Откройте браузер
2. Перейдите по адресу: http://127.0.0.1:8000/admin/
3. Войдите с учетными данными суперпользователя
4. В левом меню найдите раздел "SHOP"
5. Нажмите на "Messages" (Сообщения)
6. Сделайте скриншот страницы со списком сообщений

**Что должно быть видно:**
- URL в адресной строке: `/admin/shop/message/`
- Заголовок "Select message to change"
- Таблица с колонками: Name, Email, User, Created at, Message preview
- Кнопка "Add message" в правом верхнем углу

---

## 2. ФОРМЫ

### Скриншот 2.1: Код формы регистрации

**Путь к файлу:** `shop/forms.py`

**Действия:**
1. Откройте файл `shop/forms.py` в редакторе
2. Найдите класс `RegistrationForm` (начинается примерно со строки 8)
3. Сделайте скриншот всего класса включая:
   - Определение класса
   - Все поля (username, email, password1, password2)
   - Класс Meta
   - Методы clean_email и clean_password2

**Что должно быть видно:**
```python
class RegistrationForm(forms.ModelForm):
    username = forms.CharField(...)
    email = forms.EmailField(...)
    password1 = forms.CharField(...)
    password2 = forms.CharField(...)
    class Meta:
        model = User
        fields = ['username', 'email']
    def clean_email(self): ...
    def clean_password2(self): ...
```

---

### Скриншот 2.2: Код формы авторизации

**Путь к файлу:** `shop/forms.py`

**Действия:**
1. В том же файле `shop/forms.py`
2. Найдите класс `LoginForm` (начинается примерно со строки 70)
3. Сделайте скриншот класса включая:
   - Наследование от `AuthenticationForm`
   - Поля username и password

**Что должно быть видно:**
```python
class LoginForm(AuthenticationForm):
    username = forms.CharField(...)
    password = forms.CharField(...)
```

---

### Скриншот 2.3: Код формы отправки сообщения

**Путь к файлу:** `shop/forms.py`

**Действия:**
1. В том же файле `shop/forms.py`
2. Найдите класс `ContactForm` (начинается примерно со строки 90)
3. Сделайте скриншот класса включая:
   - Все поля (name, email, message)
   - Класс Meta
   - Метод clean_message

**Что должно быть видно:**
```python
class ContactForm(forms.ModelForm):
    name = forms.CharField(...)
    email = forms.EmailField(...)
    message = forms.CharField(...)
    class Meta:
        model = Message
        fields = ['name', 'email', 'message']
    def clean_message(self): ...
```

---

### Скриншот 2.4: Страница регистрации (форма)

**URL:** http://127.0.0.1:8000/register/

**Действия:**
1. Откройте браузер
2. Перейдите по адресу: http://127.0.0.1:8000/register/
3. Сделайте скриншот всей страницы

**Что должно быть видно:**
- URL в адресной строке: `/register/`
- Заголовок "Регистрация"
- Форма с полями:
  - Имя пользователя
  - Электронная почта
  - Пароль
  - Подтверждение пароля
- Кнопки "Зарегистрироваться" и "Уже есть аккаунт? Войти"
- Навигационное меню в шапке сайта

---

### Скриншот 2.5: Страница авторизации (форма)

**URL:** http://127.0.0.1:8000/login/

**Действия:**
1. В браузере перейдите по адресу: http://127.0.0.1:8000/login/
2. Сделайте скриншот всей страницы

**Что должно быть видно:**
- URL в адресной строке: `/login/`
- Заголовок "Авторизация"
- Форма с полями:
  - Имя пользователя
  - Пароль
- Кнопки "Войти" и "Нет аккаунта? Зарегистрироваться"

---

### Скриншот 2.6: Страница отправки сообщения (форма)

**URL:** http://127.0.0.1:8000/contact/

**Действия:**
1. В браузере перейдите по адресу: http://127.0.0.1:8000/contact/
2. Сделайте скриншот всей страницы

**Что должно быть видно:**
- URL в адресной строке: `/contact/`
- Заголовок "Связаться с нами"
- Форма с полями:
  - Имя
  - Электронная почта
  - Сообщение (текстовое поле)
- Кнопка "Отправить сообщение"

---

## 3. ВАЛИДАЦИЯ ФОРМ

### Скриншот 3.1: Код валидации email в RegistrationForm

**Путь к файлу:** `shop/forms.py`

**Действия:**
1. Откройте файл `shop/forms.py`
2. Найдите метод `clean_email()` внутри класса `RegistrationForm` (примерно строка 50)
3. Сделайте скриншот метода целиком

**Что должно быть видно:**
```python
def clean_email(self):
    """Проверка уникальности email."""
    email = self.cleaned_data.get('email')
    if User.objects.filter(email=email).exists():
        raise ValidationError('Пользователь с таким email уже зарегистрирован.')
    return email
```

---

### Скриншот 3.2: Код валидации паролей в RegistrationForm

**Путь к файлу:** `shop/forms.py`

**Действия:**
1. В том же файле `shop/forms.py`
2. Найдите метод `clean_password2()` внутри класса `RegistrationForm` (примерно строка 58)
3. Сделайте скриншот метода целиком

**Что должно быть видно:**
```python
def clean_password2(self):
    """Проверка совпадения паролей."""
    password1 = self.cleaned_data.get('password1')
    password2 = self.cleaned_data.get('password2')
    
    if password1 and password2 and password1 != password2:
        raise ValidationError('Пароли не совпадают.')
    
    if password1 and len(password1) < 8:
        raise ValidationError('Пароль должен содержать минимум 8 символов.')
    
    return password2
```

---

### Скриншот 3.3: Ошибка валидации - несовпадающие пароли

**URL:** http://127.0.0.1:8000/register/

**Действия:**
1. Откройте браузер
2. Перейдите на: http://127.0.0.1:8000/register/
3. Заполните форму:
   - Имя пользователя: `testuser`
   - Email: `test@example.com`
   - Пароль: `password123`
   - Подтверждение пароля: `password456` (другой пароль!)
4. Нажмите кнопку "Зарегистрироваться"
5. Сделайте скриншот страницы с ошибкой

**Что должно быть видно:**
- URL в адресной строке: `/register/`
- Сообщение об ошибке: "Пароли не совпадают." (красным цветом)
- Форма все еще заполнена (данные не потеряны)

---

### Скриншот 3.4: Ошибка валидации - существующий email

**URL:** http://127.0.0.1:8000/register/

**Действия:**
1. Сначала зарегистрируйте пользователя с email `test@example.com` (если еще не зарегистрирован)
2. Выйдите из системы (если авторизованы)
3. Перейдите на: http://127.0.0.1:8000/register/
4. Заполните форму:
   - Имя пользователя: `anotheruser`
   - Email: `test@example.com` (тот же email!)
   - Пароль: `password123`
   - Подтверждение пароля: `password123`
5. Нажмите кнопку "Зарегистрироваться"
6. Сделайте скриншот страницы с ошибкой

**Что должно быть видно:**
- URL в адресной строке: `/register/`
- Сообщение об ошибке: "Пользователь с таким email уже зарегистрирован." (красным цветом)

---

### Скриншот 3.5: Код валидации сообщения в ContactForm

**Путь к файлу:** `shop/forms.py`

**Действия:**
1. Откройте файл `shop/forms.py`
2. Найдите метод `clean_message()` внутри класса `ContactForm` (примерно строка 130)
3. Сделайте скриншот метода целиком

**Что должно быть видно:**
```python
def clean_message(self):
    """Валидация текста сообщения."""
    message = self.cleaned_data.get('message')
    
    if not message or not message.strip():
        raise ValidationError('Сообщение не может быть пустым.')
    
    if len(message) > 500:
        raise ValidationError('Сообщение не должно превышать 500 символов.')
    
    return message.strip()
```

---

### Скриншот 3.6: Ошибка валидации - пустое сообщение

**URL:** http://127.0.0.1:8000/contact/

**Действия:**
1. Откройте браузер
2. Перейдите на: http://127.0.0.1:8000/contact/
3. Заполните форму:
   - Имя: `Иван`
   - Email: `ivan@example.com`
   - Сообщение: оставьте ПУСТЫМ
4. Нажмите кнопку "Отправить сообщение"
5. Сделайте скриншот страницы с ошибкой

**Что должно быть видно:**
- URL в адресной строке: `/contact/`
- Сообщение об ошибке: "Сообщение не может быть пустым." (красным цветом)

---

### Скриншот 3.7: Ошибка валидации - сообщение длиннее 500 символов

**URL:** http://127.0.0.1:8000/contact/

**Действия:**
1. Откройте браузер
2. Перейдите на: http://127.0.0.1:8000/contact/
3. Заполните форму:
   - Имя: `Иван`
   - Email: `ivan@example.com`
   - Сообщение: введите текст длиннее 500 символов (можно скопировать длинный текст или написать много букв)
4. Нажмите кнопку "Отправить сообщение"
5. Сделайте скриншот страницы с ошибкой

**Что должно быть видно:**
- URL в адресной строке: `/contact/`
- Сообщение об ошибке: "Сообщение не должно превышать 500 символов." (красным цветом)

---

## 4. ОБРАБОТКА ФОРМ

### Скриншот 4.1: Код представления register

**Путь к файлу:** `shop/views.py`

**Действия:**
1. Откройте файл `shop/views.py` в редакторе
2. Найдите функцию `register()` (начинается примерно со строки 81)
3. Сделайте скриншот функции целиком

**Что должно быть видно:**
```python
def register(request):
    """
    Представление для регистрации нового пользователя.
    ...
    """
    if request.user.is_authenticated:
        return redirect('shop:home')
    
    if request.method == 'POST':
        form = RegistrationForm(request.POST)
        if form.is_valid():
            user = form.save()
            messages.success(request, 'Регистрация прошла успешно! Теперь вы можете войти.')
            return redirect('shop:registration_success')
    else:
        form = RegistrationForm()
    
    context = {
        'form': form,
        'page_title': 'Регистрация',
    }
    return render(request, 'shop/register.html', context)
```

---

### Скриншот 4.2: Страница успешной регистрации

**URL:** http://127.0.0.1:8000/registration-success/

**Действия:**
1. Откройте браузер
2. Перейдите на: http://127.0.0.1:8000/register/
3. Заполните форму регистрации с валидными данными:
   - Имя пользователя: `newuser`
   - Email: `newuser@example.com`
   - Пароль: `password123`
   - Подтверждение пароля: `password123`
4. Нажмите "Зарегистрироваться"
5. После перенаправления сделайте скриншот страницы успеха

**Что должно быть видно:**
- URL в адресной строке: `/registration-success/`
- Заголовок "🎉 Регистрация прошла успешно!"
- Текст "Вы успешно зарегистрировались в системе."
- Кнопки "Войти" и "На главную"

---

### Скриншот 4.3: Код представления login_view

**Путь к файлу:** `shop/views.py`

**Действия:**
1. Откройте файл `shop/views.py`
2. Найдите функцию `login_view()` (начинается примерно со строки 100)
3. Сделайте скриншот функции целиком

**Что должно быть видно:**
```python
def login_view(request):
    """
    Представление для авторизации пользователя.
    ...
    """
    if request.user.is_authenticated:
        return redirect('shop:home')
    
    if request.method == 'POST':
        form = LoginForm(request, data=request.POST)
        if form.is_valid():
            username = form.cleaned_data.get('username')
            password = form.cleaned_data.get('password')
            user = authenticate(username=username, password=password)
            if user is not None:
                login(request, user)
                messages.success(request, f'Добро пожаловать, {username}!')
                return redirect('shop:home')
    ...
```

---

### Скриншот 4.4: Главная страница после авторизации

**URL:** http://127.0.0.1:8000/home/

**Действия:**
1. Откройте браузер
2. Перейдите на: http://127.0.0.1:8000/login/
3. Введите учетные данные существующего пользователя:
   - Имя пользователя: `testuser` (или ваш пользователь)
   - Пароль: `password123` (или ваш пароль)
4. Нажмите "Войти"
5. После перенаправления сделайте скриншот главной страницы

**Что должно быть видно:**
- URL в адресной строке: `/home/`
- Заголовок "Добро пожаловать, [имя пользователя]!"
- Текст "Вы успешно авторизованы в системе."
- Список возможностей (просмотр каталога, отправка сообщений и т.д.)
- Кнопки быстрых действий

---

### Скриншот 4.5: Успешная отправка сообщения (обычная форма)

**URL:** http://127.0.0.1:8000/contact/

**Действия:**
1. Откройте браузер
2. Перейдите на: http://127.0.0.1:8000/contact/
3. Заполните форму:
   - Имя: `Иван Иванов`
   - Email: `ivan@example.com`
   - Сообщение: `Тестовое сообщение для проверки работы формы.`
4. Нажмите "Отправить сообщение"
5. Сделайте скриншот страницы с сообщением об успехе

**Что должно быть видно:**
- URL в адресной строке: `/contact/`
- Зеленое сообщение об успехе: "Ваше сообщение успешно отправлено!"
- Форма очищена (или заполнена заново)

---

## 5. AJAX ДЛЯ ОТПРАВКИ СООБЩЕНИЙ

### Скриншот 5.1: Код Ajax-обработчика contact_ajax

**Путь к файлу:** `shop/views.py`

**Действия:**
1. Откройте файл `shop/views.py`
2. Найдите функцию `contact_ajax()` (начинается примерно со строки 193)
3. Сделайте скриншот функции целиком

**Что должно быть видно:**
```python
@require_http_methods(["POST"])
def contact_ajax(request):
    """
    Ajax-обработчик для отправки сообщения без перезагрузки страницы.
    ...
    """
    form = ContactForm(request.POST)
    
    if form.is_valid():
        message = form.save(commit=False)
        if request.user.is_authenticated:
            message.user = request.user
        message.save()
        
        return JsonResponse({
            'success': True,
            'message': 'Ваше сообщение успешно отправлено!'
        })
    else:
        errors = {}
        for field, error_list in form.errors.items():
            errors[field] = error_list
        
        return JsonResponse({
            'success': False,
            'errors': errors
        }, status=400)
```

---

### Скриншот 5.2: JavaScript код Ajax-запроса

**Путь к файлу:** `templates/shop/contact.html`

**Действия:**
1. Откройте файл `templates/shop/contact.html` в редакторе
2. Прокрутите вниз до тега `<script>` (примерно строка 60)
3. Сделайте скриншот JavaScript кода целиком (весь блок script)

**Что должно быть видно:**
```javascript
document.getElementById('contact-form').addEventListener('submit', function(e) {
    e.preventDefault();
    
    const form = this;
    const formData = new FormData(form);
    const submitBtn = document.getElementById('submit-btn');
    const messageDiv = document.getElementById('ajax-message');
    
    submitBtn.disabled = true;
    submitBtn.textContent = 'Отправка...';
    
    // ... остальной код Ajax запроса
    fetch('{% url "shop:contact_ajax" %}', {
        method: 'POST',
        body: formData,
        headers: {
            'X-CSRFToken': csrftoken
        }
    })
    ...
});
```

---

### Скриншот 5.3: Успешная отправка через Ajax (без перезагрузки)

**URL:** http://127.0.0.1:8000/contact/

**Действия:**
1. Откройте браузер
2. Перейдите на: http://127.0.0.1:8000/contact/
3. **ВАЖНО:** Откройте инструменты разработчика (F12 или ПКМ → Inspect)
4. Перейдите на вкладку "Network" (Сеть)
5. Заполните форму:
   - Имя: `Петр Петров`
   - Email: `petr@example.com`
   - Сообщение: `Тестовое сообщение через Ajax`
6. Нажмите "Отправить сообщение"
7. **Сделайте скриншот БЕЗ закрытия DevTools**, чтобы было видно:
   - Сообщение об успехе появилось БЕЗ перезагрузки страницы
   - URL остался `/contact/` (не изменился)
   - Вкладка Network показывает Ajax-запрос

**Что должно быть видно:**
- URL в адресной строке: `/contact/` (не изменился!)
- Зеленое сообщение об успехе появилось без перезагрузки
- Форма очищена
- В DevTools виден запрос к `/contact/ajax/` со статусом 200

---

### Скриншот 5.4: Ошибка валидации через Ajax

**URL:** http://127.0.0.1:8000/contact/

**Действия:**
1. Откройте браузер
2. Перейдите на: http://127.0.0.1:8000/contact/
3. Заполните форму с ошибкой:
   - Имя: `Тест`
   - Email: `test@example.com`
   - Сообщение: оставьте ПУСТЫМ
4. Нажмите "Отправить сообщение"
5. Сделайте скриншот страницы с ошибкой

**Что должно быть видно:**
- URL в адресной строке: `/contact/` (не изменился!)
- Красное сообщение об ошибке появилось БЕЗ перезагрузки страницы
- Текст ошибки: "Ошибки валидации: Сообщение не может быть пустым."

---

### Скриншот 5.5: Инструменты разработчика - Network tab

**URL:** http://127.0.0.1:8000/contact/

**Действия:**
1. Откройте браузер
2. Перейдите на: http://127.0.0.1:8000/contact/
3. Откройте DevTools (F12)
4. Перейдите на вкладку **Network** (Сеть)
5. Очистите список запросов (кнопка "Clear" или Ctrl+L)
6. Заполните форму и отправьте сообщение
7. В списке запросов найдите запрос к `contact/ajax/`
8. Кликните на этот запрос
9. Сделайте скриншот с открытыми вкладками:
   - **Headers** - показывающий POST запрос
   - **Response** - показывающий JSON ответ `{"success": true, "message": "..."}`

**Что должно быть видно:**
- Вкладка Network с запросом `contact/ajax/`
- Метод: POST
- Статус: 200 OK
- Response содержит JSON: `{"success": true, "message": "Ваше сообщение успешно отправлено!"}`

---

## 6. ШАБЛОНЫ

### Скриншот 6.1: Базовый шаблон base.html

**Путь к файлу:** `templates/shop/base.html`

**Действия:**
1. Откройте файл `templates/shop/base.html` в редакторе
2. Сделайте скриншот начала файла, показывающий:
   - `{% extends %}` (если есть)
   - Структуру с блоками `{% block title %}`, `{% block content %}`

**Что должно быть видно:**
```html
{% load static %}
<!DOCTYPE html>
<html lang="ru">
<head>
    ...
    <title>{% block title %}Интернет-магазин{% endblock %}</title>
    ...
</head>
<body>
    ...
    {% block content %}
    {% endblock %}
    ...
</body>
</html>
```

---

### Скриншот 6.2: Шаблон регистрации с наследованием

**Путь к файлу:** `templates/shop/register.html`

**Действия:**
1. Откройте файл `templates/shop/register.html` в редакторе
2. Сделайте скриншот начала файла (первые 10-15 строк)

**Что должно быть видно:**
```html
{% extends "shop/base.html" %}
{% load static %}

{% block title %}Регистрация - {{ block.super }}{% endblock %}

{% block content %}
    <div class="form-container">
        <h1 class="page-title">Регистрация</h1>
        ...
```

---

### Скриншот 6.3: Страница профиля пользователя

**URL:** http://127.0.0.1:8000/profile/

**Действия:**
1. **ВАЖНО:** Сначала авторизуйтесь в системе
2. Откройте браузер
3. Перейдите на: http://127.0.0.1:8000/profile/
4. Сделайте скриншот всей страницы

**Что должно быть видно:**
- URL в адресной строке: `/profile/`
- Заголовок "Профиль пользователя"
- Раздел "Информация о пользователе" с данными:
  - Имя пользователя
  - Email
  - Дата регистрации
  - Последний вход
- Раздел "Мои сообщения" со списком отправленных сообщений (если есть)

---

### Скриншот 6.4: CSS стили для форм

**Путь к файлу:** `static/shop/css/styles.css`

**Действия:**
1. Откройте файл `static/shop/css/styles.css` в редакторе
2. Найдите раздел `/* ===== ФОРМЫ ===== */` (примерно строка 370)
3. Сделайте скриншот стилей для форм:
   - `.form-container`
   - `.form-group`
   - `.form-control`
   - `.form-label`

**Что должно быть видно:**
```css
/* ===== ФОРМЫ ===== */
.form-container {
    max-width: 600px;
    margin: 0 auto;
    background: white;
    padding: 2rem;
    border-radius: 8px;
    box-shadow: 0 2px 8px rgba(0,0,0,0.1);
}

.form-group {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
}
...
```

---

### Скриншот 6.5: Визуальное оформление форм

**URL:** http://127.0.0.1:8000/register/ (или любая другая страница с формой)

**Действия:**
1. Откройте браузер
2. Перейдите на: http://127.0.0.1:8000/register/
3. Сделайте скриншот страницы, показывающий красивое оформление формы

**Что должно быть видно:**
- Красиво оформленная форма с закругленными углами
- Стилизованные поля ввода
- Кнопки с hover-эффектом (можно показать при наведении)
- Единый стиль с остальным сайтом

---

## 7. МАРШРУТЫ

### Скриншот 7.1: URL-маршруты приложения

**Путь к файлу:** `shop/urls.py`

**Действия:**
1. Откройте файл `shop/urls.py` в редакторе
2. Сделайте скриншот всего файла целиком

**Что должно быть видно:**
```python
app_name = 'shop'

urlpatterns = [
    path('', views.product_list, name='product_list'),
    path('product/<int:product_id>/', views.product_detail, name='product_detail'),
    path('register/', views.register, name='register'),
    path('registration-success/', views.registration_success, name='registration_success'),
    path('login/', views.login_view, name='login'),
    path('logout/', views.logout_view, name='logout'),
    path('home/', views.home, name='home'),
    path('contact/', views.contact, name='contact'),
    path('contact/ajax/', views.contact_ajax, name='contact_ajax'),
    path('profile/', views.profile, name='profile'),
]
```

---

### Скриншот 7.2: Навигация в шаблоне base.html

**Путь к файлу:** `templates/shop/base.html`

**Действия:**
1. Откройте файл `templates/shop/base.html` в редакторе
2. Найдите раздел с навигацией (примерно строки 18-32)
3. Сделайте скриншот навигационного меню

**Что должно быть видно:**
```html
<nav class="nav">
    <ul class="nav-list">
        <li><a href="{% url 'shop:product_list' %}" class="nav-link">Каталог товаров</a></li>
        {% if user.is_authenticated %}
            <li><a href="{% url 'shop:home' %}" class="nav-link">Главная</a></li>
            <li><a href="{% url 'shop:profile' %}" class="nav-link">Профиль</a></li>
            ...
        {% else %}
            <li><a href="{% url 'shop:register' %}" class="nav-link">Регистрация</a></li>
            <li><a href="{% url 'shop:login' %}" class="nav-link">Войти</a></li>
            ...
        {% endif %}
    </ul>
</nav>
```

---

### Скриншот 7.3: Работающие URL-адреса

**Действия:**
Сделайте несколько скриншотов разных страниц с видимым URL:

1. **Скриншот 7.3.1:** http://127.0.0.1:8000/register/
   - Покажите URL в адресной строке

2. **Скриншот 7.3.2:** http://127.0.0.1:8000/login/
   - Покажите URL в адресной строке

3. **Скриншот 7.3.3:** http://127.0.0.1:8000/home/
   - Покажите URL в адресной строке (после авторизации)

4. **Скриншот 7.3.4:** http://127.0.0.1:8000/contact/
   - Покажите URL в адресной строке

5. **Скриншот 7.3.5:** http://127.0.0.1:8000/profile/
   - Покажите URL в адресной строке (после авторизации)

---

## 8. ДОКУМЕНТАЦИЯ

### Скриншот 8.1: Файл документации

**Путь к файлу:** `ДОКУМЕНТАЦИЯ_ДЗ.md`

**Действия:**
1. Откройте файл `ДОКУМЕНТАЦИЯ_ДЗ.md` в редакторе или просмотрщике
2. Сделайте скриншот начала файла с заголовком и описанием проекта

**Что должно быть видно:**
- Заголовок "# Документация проекта: Многофункциональная форма с использованием Django"
- Описание проекта
- Начало раздела "Установка и запуск"

---

### Скриншот 8.2: Раздел "Установка и запуск"

**Путь к файлу:** `ДОКУМЕНТАЦИЯ_ДЗ.md`

**Действия:**
1. В том же файле `ДОКУМЕНТАЦИЯ_ДЗ.md`
2. Прокрутите до раздела "## Установка и запуск"
3. Сделайте скриншот этого раздела

**Что должно быть видно:**
- Заголовок "## Установка и запуск"
- Подразделы с инструкциями:
  - Установка зависимостей
  - Применение миграций
  - Создание суперпользователя
  - Запуск сервера
  - Открытие в браузере

---

## ДОПОЛНИТЕЛЬНЫЕ СКРИНШОТЫ

### Скриншот Д1: Структура директорий проекта

**Действия:**
1. Откройте проводник Windows (или файловый менеджер)
2. Перейдите в папку проекта: `C:\Users\sened\Desktop\Обучение_GPT_PD\lesson_17_Templates. Работа с шаблонами`
3. Разверните основные папки:
   - `myshop/`
   - `shop/`
   - `templates/shop/`
   - `static/shop/`
4. Сделайте скриншот структуры папок

**Что должно быть видно:**
- Основные директории проекта
- Файлы `manage.py`, `requirements.txt`
- Структура приложения `shop/`
- Папки `templates/` и `static/`

---

### Скриншот Д2: Модель Message в админке (форма добавления)

**URL:** http://127.0.0.1:8000/admin/shop/message/add/

**Действия:**
1. Откройте браузер
2. Перейдите на: http://127.0.0.1:8000/admin/
3. Войдите с учетными данными суперпользователя
4. Перейдите в SHOP → Messages
5. Нажмите "Add message" (Добавить сообщение)
6. Сделайте скриншот формы добавления сообщения

**Что должно быть видно:**
- URL в адресной строке: `/admin/shop/message/add/`
- Форма с полями: User, Name, Email, Message, Created at
- Кнопки "Save" и "Save and add another"

---

### Скриншот Д3: Миграции

**Действия:**
1. Откройте терминал/командную строку
2. Перейдите в директорию проекта:
   ```bash
   cd "C:\Users\sened\Desktop\Обучение_GPT_PD\lesson_17_Templates. Работа с шаблонами"
   ```
3. Выполните команду:
   ```bash
   python manage.py showmigrations shop
   ```
4. Сделайте скриншот вывода команды

**Что должно быть видно:**
- Список миграций приложения shop
- `[X] 0001_initial` (отмечена как примененная)
- `[X] 0002_message` (отмечена как примененная)

---

## ИТОГОВЫЙ ЧЕКЛИСТ

Перед сдачей проверьте, что у вас есть скриншоты:

### Обязательные (минимум):
- [ ] Код модели Message
- [ ] Код всех трех форм
- [ ] Код валидации (3 метода)
- [ ] Страницы с формами (3 страницы)
- [ ] Ошибки валидации (2-3 примера)
- [ ] Страница профиля
- [ ] URL-маршруты
- [ ] JavaScript Ajax код
- [ ] Работа Ajax (без перезагрузки)
- [ ] Документация

### Дополнительные (для полноты):
- [ ] Админ-панель с Message
- [ ] Страница успешной регистрации
- [ ] Главная страница после авторизации
- [ ] CSS стили
- [ ] Network tab в DevTools

---

## СОВЕТЫ ПО СЪЕМКЕ СКРИНШОТОВ

1. **Используйте одинаковое разрешение** для всех скриншотов интерфейса
2. **Показывайте URL** в адресной строке браузера
3. **Делайте скриншоты кода с подсветкой синтаксиса** (VS Code, PyCharm)
4. **Используйте полноэкранный режим** для скриншотов страниц
5. **Группируйте скриншоты** в документе по темам
6. **Добавляйте подписи** к каждому скриншоту

---

## БЫСТРЫЙ СПИСОК URL ДЛЯ ПРОВЕРКИ

- http://127.0.0.1:8000/ - Каталог товаров
- http://127.0.0.1:8000/register/ - Регистрация
- http://127.0.0.1:8000/login/ - Авторизация
- http://127.0.0.1:8000/home/ - Главная (требуется авторизация)
- http://127.0.0.1:8000/contact/ - Форма сообщения
- http://127.0.0.1:8000/profile/ - Профиль (требуется авторизация)
- http://127.0.0.1:8000/admin/ - Админ-панель

---

Удачи с созданием скриншотов! 🎯

