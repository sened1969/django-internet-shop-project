# Реализация моделей для ДЗ "Тема #19. Начало работы с Моделями"

## ✅ Статус: ВСЕ ТРЕБОВАНИЯ ВЫПОЛНЕНЫ

---

## 📋 Реализованные модели

### 1. ✅ Category (Категория товаров)

**Расположение**: `shop/models.py`

**Поля**:
- `name` (CharField) - Название категории (обязательное)
- `description` (TextField) - Описание категории (необязательное)
- `parent` (ForeignKey на 'self') - Родительская категория для вложенных категорий

**Особенности**:
- ✅ Поддержка вложенных категорий через self-referencing ForeignKey
- ✅ Метод `__str__()` показывает иерархию категорий
- ✅ Зарегистрирована в админ-панели с отображением количества товаров

---

### 2. ✅ Product (Товар) - РАСШИРЕНА

**Расположение**: `shop/models.py`

**Новые поля**:
- `category` (ForeignKey на Category) - Связь с категорией товара
- `stock` (IntegerField) - Количество товара на складе

**Обновлённый Meta класс**:
- ✅ `db_table = 'shop_products'` - Кастомное имя таблицы
- ✅ `unique_together = ['name', 'category']` - Уникальность названия в рамках категории
- ✅ `ordering = ['-created_at']` - Сортировка по дате (новые сначала)
- ✅ `verbose_name` и `verbose_name_plural` - Человеко-читаемые имена

**Совместимость**:
- ✅ Поле `category` имеет `null=True, blank=True` для обратной совместимости
- ✅ Существующие товары остались без изменений

---

### 3. ✅ Order (Заказ)

**Расположение**: `shop/models.py`

**Поля**:
- `user` (ForeignKey на User) - Пользователь, оформивший заказ
- `created_at` (DateTimeField) - Дата создания заказа (автоматически)
- `status` (CharField с choices) - Статус заказа:
  - 'processing' - В обработке
  - 'shipping' - Доставляется
  - 'delivered' - Доставлено

**Методы**:
- ✅ `total_price()` - Вычисляет общую стоимость заказа, суммируя стоимость всех OrderItem

**Особенности**:
- ✅ Использует `related_name='orders'` для доступа через `user.orders`
- ✅ Использует `related_name='items'` для доступа к товарам через `order.items`

---

### 4. ✅ OrderItem (Товар в заказе)

**Расположение**: `shop/models.py`

**Поля**:
- `order` (ForeignKey на Order) - Заказ
- `product` (ForeignKey на Product) - Товар
- `quantity` (IntegerField) - Количество товара в заказе

**Особенности**:
- ✅ Валидация количества через `MinValueValidator(1)`
- ✅ Используется для вычисления общей стоимости заказа

---

### 5. ✅ Review (Отзыв)

**Расположение**: `shop/models.py`

**Поля**:
- `product` (ForeignKey на Product) - Товар, на который оставлен отзыв
- `user` (ForeignKey на User) - Пользователь, оставивший отзыв
- `rating` (IntegerField) - Оценка от 1 до 5
- `text` (TextField) - Текст отзыва
- `created_at` (DateTimeField) - Дата создания отзыва (автоматически)

**Валидация**:
- ✅ `MinValueValidator(1)` и `MaxValueValidator(5)` для оценки

**Особенности**:
- ✅ Использует `related_name='reviews'` для доступа через `product.reviews` и `user.reviews`

---

### 6. ✅ Cart (Корзина) - БОНУС

**Расположение**: `shop/models.py`

**Поля**:
- `user` (OneToOneField на User) - Владелец корзины
- `created_at` (DateTimeField) - Дата создания корзины
- `updated_at` (DateTimeField) - Дата последнего обновления

**Особенности**:
- ✅ OneToOneField обеспечивает одну корзину на пользователя
- ✅ Автоматическое обновление `updated_at` при изменении

---

### 7. ✅ CartItem (Товар в корзине) - БОНУС

**Расположение**: `shop/models.py`

**Поля**:
- `cart` (ForeignKey на Cart) - Корзина
- `product` (ForeignKey на Product) - Товар
- `quantity` (IntegerField) - Количество товара

**Особенности**:
- ✅ `unique_together = ['cart', 'product']` - Один товар может быть только один раз в корзине
- ✅ Валидация количества через `MinValueValidator(1)`

---

## 🔧 Настройка админ-панели

**Расположение**: `shop/admin.py`

Все модели зарегистрированы в админ-панели с настройками:

### CategoryAdmin
- Отображение: название, родительская категория, количество товаров
- Фильтры: по родительской категории
- Поиск: по названию и описанию

### ProductAdmin (обновлён)
- Отображение: название, категория, цена, склад, дата создания, наличие изображения
- Фильтры: по категории и дате создания
- Поиск: по названию и описанию
- Добавлены поля: `category` и `stock` в fieldsets

### OrderAdmin
- Отображение: ID, пользователь, статус, общая стоимость, дата создания
- Фильтры: по статусу и дате создания
- Поиск: по имени пользователя и email
- Инлайн-редактор: OrderItemInline для добавления товаров в заказ

### OrderItemAdmin
- Отображение: заказ, товар, количество, стоимость товара
- Фильтры: по заказу
- Поиск: по названию товара и имени пользователя

### ReviewAdmin
- Отображение: товар, пользователь, оценка, дата создания, предпросмотр текста
- Фильтры: по оценке и дате создания
- Поиск: по названию товара, имени пользователя и тексту отзыва

### CartAdmin
- Отображение: пользователь, количество товаров, даты создания и обновления
- Поиск: по имени пользователя и email
- Инлайн-редактор: CartItemInline для добавления товаров в корзину

### CartItemAdmin
- Отображение: корзина, товар, количество, стоимость товара
- Фильтры: по корзине
- Поиск: по названию товара и имени пользователя

---

## 📊 Миграции

**Созданная миграция**: `0003_cart_order_product_stock_alter_message_message_and_more.py`

**Выполненные операции**:
1. ✅ Создана модель Cart
2. ✅ Создана модель Order
3. ✅ Добавлено поле stock в Product
4. ✅ Изменено поле message в Message (удалён max_length)
5. ✅ Переименована таблица Product в shop_products
6. ✅ Создана модель Review
7. ✅ Создана модель OrderItem
8. ✅ Создана модель Category
9. ✅ Добавлено поле category в Product
10. ✅ Изменён unique_together для Product
11. ✅ Создана модель CartItem

**Статус**: ✅ Миграции успешно применены

---

## 📝 Соответствие требованиям ДЗ

### Часть 1: Модели для интернет-магазина

- ✅ **Category**: Создана с полями name, description, parent (self-referencing)
- ✅ **Product**: Расширена полями category (ForeignKey), stock (IntegerField)
- ✅ **Order**: Создана с полями user, created_at, status (choices), методом total_price()
- ✅ **OrderItem**: Создана с полями product, order, quantity
- ✅ **Review**: Создана с полями product, user, rating (1-5), text, created_at

### Часть 2: Мета-классы и работа с базой данных

- ✅ **Мета-класс Product**: 
  - `db_table = 'shop_products'`
  - `ordering = ['-created_at']`
  - `verbose_name` и `verbose_name_plural`
- ✅ **Уникальные ограничения**: `unique_together = ['name', 'category']`
- ✅ **Метод total_price()**: Реализован в модели Order

### Часть 3: Работа с Django ORM

- ✅ Примеры запросов созданы в файле `ПРИМЕРЫ_ЗАПРОСОВ_DJANGO_ORM.md`

### Часть 4: Бонусное задание

- ✅ **Cart**: Создана модель корзины
- ✅ **CartItem**: Создана модель товара в корзине

---

## 🎯 Использование

### Создание данных через Django Shell

```bash
python manage.py shell
```

Примеры использования находятся в файле `ПРИМЕРЫ_ЗАПРОСОВ_DJANGO_ORM.md`.

### Доступ к админ-панели

```bash
python manage.py runserver
```

Откройте http://127.0.0.1:8000/admin/ и войдите в систему.

Все модели доступны для управления через админ-панель.

---

## ⚠️ Важные замечания

1. **Существующие товары**: Товары, созданные до добавления поля `category`, имеют `category=None`. Их можно обновить через админ-панель или Django Shell.

2. **Уникальность**: Название товара должно быть уникальным в рамках категории. Товары с одинаковым названием могут существовать в разных категориях.

3. **Метод total_price()**: Автоматически вычисляет стоимость заказа на основе товаров в OrderItem. Используйте `order.total_price()` для получения общей стоимости.

4. **Корзина**: Каждый пользователь имеет одну корзину (OneToOneField). Корзина создаётся автоматически при первом обращении.

5. **Валидация**: 
   - Оценка в Review должна быть от 1 до 5
   - Количество в OrderItem и CartItem должно быть >= 1
   - Stock в Product должен быть >= 0

---

## 📚 Дополнительные файлы

1. **АНАЛИЗ_ИНТЕГРАЦИИ_ТЕМА_19.md** - Подробный анализ совместимости
2. **ПРИМЕРЫ_ЗАПРОСОВ_DJANGO_ORM.md** - Примеры запросов для Части 3 ДЗ
3. **РЕАЛИЗАЦИЯ_МОДЕЛЕЙ_ТЕМА_19.md** - Этот файл

---

**Дата реализации**: 2025-01-27  
**Статус**: ✅ ВСЕ ТРЕБОВАНИЯ ВЫПОЛНЕНЫ

